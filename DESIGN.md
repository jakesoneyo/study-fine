# DESIGN.md — study-fine

## 최종 선택

**시안 B** 확정.

| 다이얼   | 값   | 근거                                                                                                                                       |
| -------- | ---- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| VARIANCE | 중간 | 로그인은 좌/우 비대칭 스플릿, 대시보드는 7:5 비대칭 그리드, 벌금 테이블은 단일 컬럼 리스트 — 섹션마다 다른 레이아웃 패밀리                 |
| MOTION   | 중간 | 인풋 포커스 링, 버튼 active scale(0.98), 토글 상태 전환, IntersectionObserver 기반 섹션 페이드업. `prefers-reduced-motion`에서 전부 무력화 |
| DENSITY  | 중간 | 벌금 기준·출석 상태·누계 다 보이되 카드 패딩·섹션 간격(88px)으로 여백 확보                                                                 |

## 팔레트 / 타이포

- 브랜드 액센트: 테라코타 `#b14f30`(라이트) / `#e07a52`(다크)
- 배경: 쿨 슬레이트 계열
- 시맨틱 상태색: 정상(그린) / 지각(앰버) / 결석(레드) — 브랜드 액센트와 분리
- 폰트: 시스템 한글 스택(-apple-system, Apple SD Gothic Neo, Malgun Gothic, Segoe UI, Roboto)
- 라운드: 카드 14px / 인풋 10px / 버튼·배지 pill(999px)
- 라이트/다크 모두 스타일링(`prefers-color-scheme` + `:root[data-theme]` 양방향), WCAG AA 대비 확인 완료

## 사용자 조정 이력

1. **좌측 브랜드 카피 줄맞춤** — 로그인 화면 좌측 패널의 헤드라인·설명문이 한글 단어 중간에서 끊기고 줄 길이가 들쭉날쭉하던 문제.
   - `word-break: keep-all` 추가 (한글 단어 단위로만 줄바꿈)
   - `text-wrap: balance`(헤드라인) / `text-wrap: pretty`(설명문)로 줄 길이 균형
   - `.login-brand-copy` max-width 26ch → 22ch로 조정, 설명문은 별도 30ch 상한

## 담은 화면 (목업 기준, 구현 시 그대로 반영)

1. 로그인 — 이메일/비밀번호 + 데모 로그인 버튼(문구 고정)
2. 운영자 대시보드 — 스터디룸명, 회차 출석 체크(정상/지각/결석 토글), 벌금 기준 표시, 예상 정산 미리보기
3. 멤버별 누적 벌금 현황 테이블 — 출석/지각/결석 횟수 + 누적 벌금

목업 원본: Artifact(https://claude.ai/code/artifact/57a5b9e1-b057-40e3-8931-5059bdc54758) — implementer가 이 HTML/CSS 구조·토큰을 React 컴포넌트로 그대로 옮긴다.
