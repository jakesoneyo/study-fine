# Ubiquitous Language — study-fine

기획 회의에서 나온 표현을 표준 용어로 정리한 문서. **SPEC / ARCHITECTURE / DATA-MODEL / API / 코드 / UI 문구는 모두 이 표에 있는 표준 용어만 쓴다.**

한국어 UI 문구는 "표준어(한)" 열, 코드·DB·API 식별자는 "표준어(영)" 열을 따른다.

---

## 조직 / 사람

| 표준어(영)     | 표준어(한) | 정의                                                                       | 쓰지 말 것                           |
| -------------- | ---------- | -------------------------------------------------------------------------- | ------------------------------------ |
| **StudyRoom**  | 스터디룸   | 이 서비스가 관리하는 단 하나의 스터디 모임. 벌금 단가를 보유한 설정 주체   | 스터디, 모임, 그룹, 방, 워크스페이스 |
| **Member**     | 멤버       | 스터디룸에 속한 참여자이자 로그인 주체(자격증명 보유)                      | 유저, 회원, 참가자, 학생, 계정       |
| **Organizer**  | 운영자     | 스터디룸을 운영하는 멤버. 출석 기록과 벌금 단가를 바꿀 수 있는 유일한 역할 | **굴리자**, 리더, 방장, 관리자, 총무 |
| **MemberRole** | 역할       | 멤버가 가지는 권한 등급. `ORGANIZER` 또는 `MEMBER` 둘 뿐                   | 등급, 권한레벨, 롤                   |

## 출석

| 표준어(영)           | 표준어(한) | 정의                                                                   | 쓰지 말 것                     |
| -------------------- | ---------- | ---------------------------------------------------------------------- | ------------------------------ |
| **StudySession**     | 회차       | 특정 날짜에 열린 스터디 모임 1회                                       | 세션, 미팅, 수업, 모임일, 일정 |
| **AttendanceRecord** | 출석 기록  | 한 회차에 대한 한 멤버의 출석 결과 1건                                 | 출결, 체크, 로그               |
| **AttendanceStatus** | 출석 상태  | 출석 기록이 가지는 값. `PRESENT`(정상) / `LATE`(지각) / `ABSENT`(결석) | 출결코드, 상태값               |
| **Check-in**         | 출석 체크  | 운영자가 한 회차의 멤버 전원 출석 상태를 한 번에 기록하는 행위         | 출석부 작성, 등록, 입력        |

## 벌금

| 표준어(영)          | 표준어(한) | 정의                                                                                     | 쓰지 말 것                     |
| ------------------- | ---------- | ---------------------------------------------------------------------------------------- | ------------------------------ |
| **FineRate**        | 벌금 단가  | 스터디룸이 보유한, 출석 상태별로 부과할 금액 설정값(`lateFineAmount`/`absentFineAmount`) | 기준값, 벌금액, 요금, 단위벌금 |
| **Fine**            | 벌금       | 출석 기록 1건에 확정되어 **저장된** 금액(`fineAmount`)                                   | 페널티, 과태료, 차감액         |
| **FinePolicy**      | 벌금 정책  | 출석 상태 + 벌금 단가 → 벌금을 산출하는 순수 규칙                                        | 계산기, 룰, 로직               |
| **AccumulatedFine** | 누적 벌금  | 한 멤버의 모든 출석 기록의 벌금 합계                                                     | 총 벌금, 미납금, 잔액, 정산액  |

---

## 관계

- **StudyRoom** 은 시스템에 **정확히 1개**만 존재한다 (단일 스터디룸 전제).
- **StudyRoom** 은 하나의 **FineRate** 를 가진다.
- **StudyRoom** 은 0..N 명의 **Member** 를 가진다.
- **Member** 는 정확히 하나의 **MemberRole** 을 가진다.
- **StudyRoom** 은 0..N 개의 **StudySession** 을 가진다.
- **StudySession** × **Member** 조합마다 **AttendanceRecord** 는 최대 1건이다.
- **AttendanceRecord** 는 정확히 1개의 **Fine** 을 확정 보유한다 (0원 포함).
- **AccumulatedFine** 은 저장되지 않는다 — **AttendanceRecord** 들의 **Fine** 합계로 항상 계산된다.

---

## 예시 대화

> **개발자:** "운영자가 **벌금 단가**를 3,000원에서 5,000원으로 올리면, 지난달 **회차**들의 **벌금**도 같이 올라가나요?"

> **도메인 전문가:** "절대 안 됩니다. 지난달 지각한 사람은 그때 기준 3,000원을 내기로 한 거예요. **벌금 단가**는 스터디룸의 현재 설정값이고, **벌금**은 **출석 체크** 시점에 확정돼서 그 **출석 기록**에 박히는 값입니다."

> **개발자:** "그럼 **누적 벌금**은 지금 **벌금 단가**로 다시 계산하는 게 아니라, 저장된 **벌금**들을 그냥 더하는 거군요."

> **도메인 전문가:** "맞습니다. 그래서 **누적 벌금**은 따로 저장할 필요가 없어요. 합계만 내면 됩니다."

> **개발자:** "**멤버**가 자기 **누적 벌금**을 볼 때 다른 사람 것도 보이나요?"

> **도메인 전문가:** "아니요. 본인 **출석 기록**과 본인 **누적 벌금**만 봅니다. 전체를 보는 건 **운영자**뿐이에요."

---

## 표준화한 모호 용어

1. **"굴리자"** — 기획 회의에서 쓰인 표현. 도메인 밖 사람에게 전달되지 않고 코드/DB 식별자로 옮기기 어렵다. → 표준어 **Organizer / 운영자** 로 고정. 역할 enum 값도 `ORGANIZER`.

2. **"멤버" vs "유저"** — 회의에서 "멤버 CRUD"와 "로그인 계정"이 따로 언급됐으나, 단일 스터디룸 전제에서는 **참여자 = 로그인 주체**로 완전히 겹친다. → **Member 하나로 통합**. `User` 라는 단어는 이 프로젝트에서 쓰지 않는다. (멀티 스터디룸으로 확장할 때 비로소 `User` ↔ `Membership` 분리가 필요해지며, 그건 명시적 비범위.)

3. **"기준값"** — 회의에서 "지각·결석 기준값"이라 표현됐으나 "기준"은 _판정 기준(몇 분부터 지각인가)_ 으로도 읽힌다. 이 프로젝트에 지각 판정 자동화는 없다(운영자가 수동 선택). → 금액을 뜻할 때는 반드시 **FineRate / 벌금 단가**.

4. **"벌금"의 두 의미** — _설정된 단가_ 와 _확정된 부과액_ 을 둘 다 "벌금"이라 부르면 위 대화의 스냅샷 규칙이 무너진다. → 설정값은 **FineRate**, 확정액은 **Fine**. 코드 필드도 `lateFineAmount`(단가) vs `fineAmount`(확정액)로 구분.

5. **"정산"** — 회의에서 "벌금 자동 정산"이라 했으나, 실제 확정 범위는 *자동 계산*까지이고 납부·이월·마감은 비범위다. → **정산(settlement)** 이라는 단어는 문서·UI에서 쓰지 않는다. **자동 계산 / 누적 벌금 조회** 로 표현.

6. **"세션"** — `StudySession`(회차)과 HTTP 세션이 충돌한다. 본 시스템은 **stateless JWT**라 HTTP 세션이 없으므로 혼동 여지는 작지만, 한국어 UI에서는 항상 **회차**로만 쓴다.
