# SPEC — study-fine

> 스터디모임 출석 관리 + 지각/결석 벌금 자동 계산
> 유형 태그: **CRUD** · 난이도 티어: **S** (하루 완성) · 백엔드: **Java 21 + Spring Boot 4**

용어는 [`UBIQUITOUS_LANGUAGE.md`](./UBIQUITOUS_LANGUAGE.md) 를 따른다.

---

## 1. 문제 정의

소규모 스터디모임은 지각·결석에 벌금을 매기는 규칙을 두지만, 실제 운영은 카톡방 + 엑셀 + 사람의 기억에 의존한다. 여기서 세 가지가 반복적으로 깨진다.

1. **집계 누락** — 누가 몇 번 지각했는지 운영자 한 명의 수기 기록에만 남는다.
2. **계산 실수** — 벌금 단가가 바뀌면 지난 회차까지 잘못 소급 계산된다.
3. **불투명성** — 멤버는 자기 벌금이 왜 그 금액인지 근거(어느 회차에 무슨 상태였는지)를 확인할 수 없다.

study-fine은 **하나의 스터디룸**에 대해 회차별 출석을 기록하고, 기록 시점의 벌금 단가로 벌금을 **확정 저장**하여, 운영자와 멤버가 각자 볼 수 있는 범위에서 근거를 조회하게 한다.

---

## 2. 목표 (성공하면 이렇게 된다)

- 운영자는 회차를 만들고 멤버 전원의 출석 상태를 **한 화면에서 한 번에** 저장한다.
- 벌금은 사람이 입력하지 않는다. 출석 상태를 고르는 순간 시스템이 계산해 확정한다.
- 벌금 단가를 나중에 바꿔도 **과거 회차의 벌금 금액은 변하지 않는다.**
- 멤버는 로그인해서 **본인** 출석 내역과 누적 벌금만 본다. 남의 것은 API로도 접근 불가.

---

## 3. 범위 (In Scope)

### 3.1 인증 / 권한

- 이메일 + 비밀번호 로그인 → JWT 발급 (stateless)
- 역할 2종: `ORGANIZER`(운영자), `MEMBER`(일반 멤버)
- **공개 회원가입 없음.** 멤버 계정은 운영자가 생성한다 (닫힌 스터디 모임이라는 도메인 특성)
- 데모 로그인: `admin` / `admin` (ORGANIZER 롤로 시드)

### 3.2 멤버 관리 — ORGANIZER 전용

- 멤버 생성 (이름, 이메일, 초기 비밀번호, 역할)
- 멤버 목록 조회 — **누적 벌금 포함**
- 멤버 수정 (이름, 역할, 활성 여부)
- 멤버 비활성화 (soft delete — 출석 이력 보존)

### 3.3 회차 관리 — ORGANIZER 전용 (조회는 전원)

- 회차 생성 (날짜, 제목)
- 회차 목록 조회
- 회차 상세 조회 (그 회차의 전체 출석 현황 + 회차 벌금 합계)
- 회차 삭제 (출석 기록 cascade 삭제)

### 3.4 출석 체크 — ORGANIZER 전용

- 한 회차에 대해 **멤버 전원의 출석 상태를 한 번의 요청으로 저장**(bulk upsert, 단일 트랜잭션)
- 상태 3종: `PRESENT` / `LATE` / `ABSENT`
- 저장 시 현재 벌금 단가로 벌금을 계산해 **출석 기록에 확정 저장**

### 3.5 벌금

- 벌금 단가(지각 단가 / 결석 단가) 조회 — 전원
- 벌금 단가 수정 — ORGANIZER 전용
- 누적 벌금: 저장된 벌금들의 합계로 **집계 쿼리 1회**로 산출 (컬럼으로 들고 있지 않음)

### 3.6 조회 격리

- ORGANIZER: 전체 멤버의 출석·벌금 조회 가능
- MEMBER: `본인` 출석 내역 + 본인 누적 벌금만 조회 가능. 타인 리소스 요청 시 **403**

### 3.7 운영 필수 요소 (CLAUDE.md S티어 규정)

- `/health` 헬스체크
- springdoc-openapi Swagger UI
- 핵심 로직 단위 테스트: **벌금 계산(FinePolicy)** + **권한 가드**
- GitHub Actions CI
- Dockerfile
- README + 아키텍처 다이어그램
- 데모 시드 데이터 (admin + 샘플 멤버 3명 + 회차 2개 + 출석 기록) → 첫 화면이 비어 보이지 않게

---

## 4. 비범위 (Out of Scope) — 명시적으로 만들지 않는다

| 제외 항목                                       | 이유                                                             |
| ----------------------------------------------- | ---------------------------------------------------------------- |
| **멀티 스터디룸 / 멀티테넌시**                  | 이미 pingboard·yuhyeon에서 다뤘음. 스터디룸은 단일 행으로 고정   |
| **벌금 납부 / 미납 / 이월 / 정산 마감**         | S티어 초과. 벌금은 "계산·누적 조회"까지만 (→ backlog M티어 확장) |
| **결제 연동 (토스/카카오페이)**                 | 외부API 유형이며 별도 프로젝트 소재                              |
| **지각 자동 판정 (입장 시각 기반)**             | 실시간 유형. 출석 상태는 운영자가 수동 선택                      |
| **공개 회원가입 / 이메일 인증 / 비밀번호 찾기** | 닫힌 모임 도메인 + S티어 스코프                                  |
| **알림 (푸시/이메일/카톡)**                     | 외부 연동 스코프                                                 |
| **출석률 통계 대시보드 / 차트**                 | 누적 벌금 + 출석 카운트 숫자까지만                               |
| **파일 업로드 / 프로필 이미지**                 | 도메인 가치 없음                                                 |
| **소셜 로그인 (카카오)**                        | 인증 고도화 유형이며 별도 프로젝트 소재                          |
| **통합 테스트 (Testcontainers)**                | CLAUDE.md 상 S티어는 면제. 단위 테스트만                         |
| **백엔드 라이브 배포 (Render)**                 | 워크스페이스 규칙: Spring은 512MB OOM 위험 → 로컬 쇼케이스       |

---

## 5. 유저 스토리

### 운영자 (ORGANIZER)

| ID  | 스토리                                                            | 인수 조건                                                                          |
| --- | ----------------------------------------------------------------- | ---------------------------------------------------------------------------------- |
| O-1 | 운영자로서, 멤버를 등록해 스터디룸 명단을 만들고 싶다             | 이메일 중복 시 409. 이메일 형식 아니면 400. 생성 즉시 해당 계정으로 로그인 가능    |
| O-2 | 운영자로서, 새 회차를 만들고 싶다                                 | 같은 날짜 회차가 이미 있으면 409                                                   |
| O-3 | 운영자로서, 한 회차의 멤버 전원 출석 상태를 한 번에 저장하고 싶다 | 활성 멤버 전원이 목록에 뜬다. 저장은 단일 트랜잭션. 재저장 시 기존 기록이 갱신된다 |
| O-4 | 운영자로서, 출석 상태를 고르면 벌금이 자동 계산되길 원한다        | PRESENT=0, LATE=지각단가, ABSENT=결석단가. 수기 입력 필드 없음                     |
| O-5 | 운영자로서, 벌금 단가를 조정하고 싶다                             | 0 이상 정수만 허용. **저장 후에도 과거 출석 기록의 벌금 금액이 바뀌지 않는다**     |
| O-6 | 운영자로서, 멤버별 누적 벌금을 한눈에 보고 싶다                   | 멤버 목록에 누적 벌금이 함께 뜬다. 멤버 N명이어도 쿼리는 상수 개 (N+1 없음)        |
| O-7 | 운영자로서, 탈퇴한 멤버를 명단에서 내리되 과거 기록은 남기고 싶다 | 비활성 멤버는 출석 체크 화면에 안 뜨지만, 과거 출석 기록과 벌금은 그대로 조회된다  |

### 멤버 (MEMBER)

| ID  | 스토리                                                  | 인수 조건                                               |
| --- | ------------------------------------------------------- | ------------------------------------------------------- |
| M-1 | 멤버로서, 내 출석 내역을 회차별로 보고 싶다             | 회차 날짜·제목·내 상태·그 건의 벌금이 최신순으로 보인다 |
| M-2 | 멤버로서, 내 누적 벌금이 얼마인지 알고 싶다             | 출석 내역 응답에 누적 벌금과 상태별 횟수가 함께 온다    |
| M-3 | 멤버로서, 다른 사람 벌금은 보이지 않아야 한다           | `/api/members`, `/api/members/{id}/**` 호출 시 **403**  |
| M-4 | 멤버로서, 출석 기록을 내가 고칠 수 없어야 한다 (신뢰성) | 쓰기 엔드포인트 전부 **403**                            |

### 면접관 / 방문자

| ID  | 스토리                                        | 인수 조건                                                                                         |
| --- | --------------------------------------------- | ------------------------------------------------------------------------------------------------- |
| V-1 | 방문자로서, 가입 없이 완성된 화면을 보고 싶다 | 로그인 화면의 **`회원가입 없이 둘러보기`** 버튼 1클릭으로 admin 로그인. 데이터가 이미 채워져 있음 |

---

## 6. 성공 기준 (Definition of Done)

**기능**

- [ ] 위 유저 스토리 인수 조건 전부 통과
- [ ] 벌금 단가를 바꾼 뒤 과거 회차 상세를 조회했을 때 금액이 그대로임을 수동 검증

**품질**

- [ ] `FinePolicy` 단위 테스트: 3개 상태 × 단가 조합 + 단가 0 + 스냅샷 불변성
- [ ] 권한 가드 테스트: MEMBER 토큰으로 ORGANIZER 엔드포인트 호출 → 403 / 토큰 없음 → 401
- [ ] `./gradlew build` 그린, 프론트 `tsc --noEmit` + `vite build` 그린
- [ ] GitHub Actions CI 그린 (백엔드 + 프론트 잡)

**운영**

- [ ] `GET /health` → 200 `{"status":"UP"}`
- [ ] `/swagger-ui.html` 에서 전 엔드포인트 확인 가능, 인증 필요 엔드포인트에 자물쇠 표시
- [ ] `docker build` 성공 + 컨테이너 기동 후 `/health` 200
- [ ] 시드 실행 후 재실행해도 중복 데이터 안 생김(멱등)
- [ ] `.env` 는 gitignore. 저장소에는 `.env.example` 만

**문서**

- [ ] README: 실행법(로컬/Docker), 아키텍처 다이어그램, 데모 계정 안내, 스크린샷/GIF
- [ ] 프론트 Vercel 라이브 URL

---

## 7. 핵심 설계 결정 (한 줄 요약, 상세는 ARCHITECTURE.md)

1. **벌금 스냅샷** — 벌금은 파생 계산이 아니라 출석 체크 시점에 확정 저장. 단가 변경의 소급 오염 차단.
2. **누적 벌금은 비저장** — 합계 컬럼을 두지 않고 집계 쿼리로 산출. 이중 갱신 버그 원천 차단.
3. **StudyRoom은 단일 행 테이블** — 벌금 단가를 하드코딩/프로퍼티가 아닌 DB에 둬서 런타임 수정 가능.
4. **Member = 참여자 + 로그인 주체 통합** — 단일 스터디룸 전제에서 User/Member 분리는 과설계.
5. **멤버 삭제는 soft delete** — 출석 이력과 벌금 근거 보존.
