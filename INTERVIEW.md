# INTERVIEW — study-fine 면접 대비

> 이 프로젝트 기반 **예상 질문 + 모범 답안**.
> 개념 설명이 필요하면 [STUDY.md](./STUDY.md)를 먼저 본다.
>
> **사용법**: 답안을 외우지 말고 **구조**를 외운다. 각 답안은 `핵심 한 줄 → 근거 → 트레이드오프` 순서로 되어 있다.
> ⚠️ 표시는 **틀리게 말하면 감점되는 지점**이다.

---

## 목차

0. [오프닝 스크립트 (1분 / 3분)](#0-오프닝-스크립트)
1. [프로젝트 전반](#1-프로젝트-전반)
2. [기술 선택 이유 (Spring Boot 4 / Security 7 첫 경험)](#2-기술-선택-이유)
3. [도메인 설계 — 벌금 스냅샷](#3-도메인-설계--벌금-스냅샷)
4. [JPA / 데이터베이스](#4-jpa--데이터베이스)
5. [보안 / 인증 / 인가](#5-보안--인증--인가)
6. [트러블슈팅 스토리 (STAR)](#6-트러블슈팅-스토리-star)
7. [프론트엔드](#7-프론트엔드)
8. [배포 / 인프라 / JVM](#8-배포--인프라--jvm)
9. [테스트 / 품질](#9-테스트--품질)
10. [★ 압박 질문 모음](#10--압박-질문-모음)
11. [내가 아는 한계 (먼저 말하기)](#11-내가-아는-한계-먼저-말하기)
12. [역질문 / 마무리](#12-역질문--마무리)
13. [절대 하면 안 되는 답변](#13-절대-하면-안-되는-답변)

---

## 0. 오프닝 스크립트

### 30초 버전 (자기소개 중 프로젝트 언급)

> "study-fine은 스터디모임 출석과 벌금을 관리하는 앱입니다. 핵심은 **벌금을 조회 시점에 계산하는 파생값이 아니라, 출석 체크 시점에 확정 저장되는 '사실'로 설계**한 겁니다. 그래서 나중에 벌금 단가를 올려도 지난 회차 금액이 소급 인상되지 않습니다. 백엔드는 **Spring Boot 4 + Java 21**로, 제 첫 Java/Spring 프로젝트입니다."

### 1분 버전

> "스터디모임의 출석·벌금 관리 앱입니다. 카톡방과 엑셀로 하던 걸 대체합니다.
>
> 이 프로젝트에서 가장 신경 쓴 건 **세 가지**입니다.
>
> 첫째, **벌금 스냅샷**입니다. 벌금을 '지금 단가로 계산하는 값'이 아니라 '그 시점에 확정된 사실'로 보고 출석 기록에 저장했습니다. 주문서에 주문 당시 가격을 박아두는 것과 같은 이유입니다. 단가 변경 서비스가 출석 리포지토리를 참조조차 못 하도록 의존 방향을 잘라서, 소급 오염 경로가 코드상 존재하지 않게 했습니다.
>
> 둘째, **N+1 방지**입니다. 멤버별 누적 벌금은 GROUP BY 프로젝션 단일 쿼리로, 회차 상세는 `@EntityGraph`로, 출석 일괄 저장은 사전 조회 후 Map으로 — 지점마다 다른 방식으로 막았습니다. 멤버가 4명이든 40명이든 쿼리 개수가 같습니다.
>
> 셋째, **소유권 격리를 비교문이 아니라 URL 구조로** 했습니다. 본인 조회는 `/api/me/attendances`처럼 id 파라미터 자체를 안 받고 토큰의 sub만 씁니다. id를 받는 경로는 통째로 운영자 전용이라, '소유권 비교를 빠뜨려서 생기는 취약점'이 구조적으로 불가능합니다.
>
> 기술적으로는 **워크스페이스의 첫 Spring 프로젝트**라 Spring Boot 4와 Security 7의 변경점을 실제로 부딪히면서 배웠고, Render 512MB 무료 티어에 JVM 앱을 올리기 위해 힙·메타스페이스를 튜닝한 경험도 있습니다."

### 3분 버전 (기술 심화 요청 시)

위 1분 + 다음을 덧붙인다:

> "구현하면서 실제로 막힌 것들이 몇 가지 있습니다.
>
> Spring Boot 4가 **Jackson 3(`tools.jackson`)** 로 이행해서, 습관대로 `com.fasterxml.jackson`을 import 했다가 빈 주입이 안 돼서 애플리케이션이 안 떴습니다. 이 문제 때문에 JWT 라이브러리도 jjwt 대신 **Spring 내장 Nimbus**를 골랐습니다 — `jjwt-jackson`이 Jackson 2 바인딩이라 같은 충돌이 났을 겁니다.
>
> Security 7에서는 **stateless REST API도 CSRF를 명시적으로 꺼주지 않으면 쓰기 요청이 전부 403**이 됩니다. 403이라 권한 문제인 줄 알고 한참 헤맸는데, 'GET은 되는데 쓰기만 403이면 CSRF부터 의심한다'는 진단 규칙을 그때 얻었습니다.
>
> 배포에서는 Render 무료 티어 512MB에 Spring을 올리는 게 원래 제 워크스페이스 규칙상 금지였는데, **힙 256MB + 메타스페이스 180MB + SerialGC** 조합으로 실측해서 396MB에 안정화시키고 예외적으로 라이브 배포했습니다. 처음엔 메타스페이스를 96MB로 잡았다가 `OutOfMemoryError: Metaspace`로 죽는 걸 보고, `-Xmx`가 힙만 제한한다는 걸 몸으로 배웠습니다.
>
> 마지막으로, 배포 전 검수에서 blocker 3건을 찾아 고쳤습니다. 그중 하나는 **React의 `key`와 제어/비제어 컴포넌트를 잘못 써서 운영자 권한이 조용히 강등되던 버그**였는데, 화면상으로는 아무 문제가 없어 보였다는 게 특히 무서웠습니다."

---

## 1. 프로젝트 전반

### Q1-1. 이 프로젝트를 소개해주세요.

→ **[0번 오프닝 스크립트](#0-오프닝-스크립트)** 참조.

**팁**: 면접관이 백엔드 중심이면 스냅샷 + N+1을, 보안 관심이면 소유권 격리를, 인프라 관심이면 JVM 튜닝을 앞으로 뺀다.

---

### Q1-2. 왜 이 주제를 골랐나요? 실제로 필요한 서비스인가요?

**답변**

> "포트폴리오 다양성 관점과 도메인 관점 두 가지 이유가 있습니다.
>
> 다양성 관점에서는, 제가 그동안 만든 프로젝트가 실시간·알고리즘·외부 API 연동 쪽에 몰려 있었고 **순수 CRUD가 하나도 없었습니다.** 그리고 백엔드가 전부 NestJS였습니다. 그래서 이번엔 **CRUD 유형 + Spring**으로 공백을 메웠습니다.
>
> 도메인 관점에서는, 스터디 벌금 관리가 '단순 CRUD처럼 보이지만 **시간에 따라 값이 변하는 설정을 어떻게 다룰 것인가**'라는 흥미로운 문제를 품고 있습니다. 벌금 단가가 바뀌면 과거 기록은 어떻게 되어야 하나 — 이게 이 프로젝트의 전부라고 해도 됩니다. 규모는 작지만 판단이 필요한 지점이 있는 주제를 고른 겁니다."

**꼬리질문: "CRUD면 별로 어렵지 않은 거 아닌가요?"**

> "기능 구현 자체는 어렵지 않습니다. 어려운 건 **CRUD를 CRUD답게 하지 않는 지점을 찾는 것**이었습니다. 예를 들어 벌금은 순진하게 하면 조회할 때 계산하면 됩니다. 그게 정규화 관점에서 더 '깨끗'해 보이기까지 합니다. 그런데 그렇게 하면 도메인 규칙이 깨집니다. 이런 판단을 **문서로 남기고 테스트로 잠그는 것**까지가 제가 이 프로젝트에서 하려던 일입니다."

---

### Q1-3. 개발 기간과 규모는요?

**답변**

> "S티어(하루 완성)로 잡고 진행했습니다. 백엔드가 시간의 약 60%, 프론트 30%, 배포·문서 10% 정도였습니다.
>
> 규모는 API 엔드포인트 15개, 테이블 4개, 테스트 28개입니다. 다만 **코딩 전에 설계 문서를 먼저 썼습니다** — SPEC, ARCHITECTURE, DATA-MODEL, API, 그리고 용어집(UBIQUITOUS_LANGUAGE)까지. 하루짜리에 문서가 과하다고 볼 수도 있는데, 실제로는 이게 시간을 **줄였습니다.** '비범위(Out of Scope)' 표를 먼저 못 박아둔 덕에 '벌금 납부 기능도 넣을까' 같은 스코프 크리프를 그때그때 차단할 수 있었습니다."

---

### Q1-4. 가장 어려웠던 부분은?

**답변** (하나만 고르되 이유를 명확히)

> "**Spring Boot 4와 Security 7이 처음이라, 인터넷에 있는 예제가 대부분 안 통했다는 것**입니다.
>
> Boot 3용 코드를 복붙하면 컴파일부터 안 되는 경우가 많았습니다. `authorizeRequests()`가 제거됐고, `starter-web`이 `starter-webmvc`로 바뀌었고, Jackson 패키지 루트가 통째로 바뀌었습니다. 그래서 **검색 결과보다 공식 마이그레이션 문서를 먼저 읽는 습관**으로 전환했습니다.
>
> 이게 어려웠지만 결과적으로 가장 많이 배운 부분이기도 합니다. '이 API가 왜 이렇게 생겼는지'를 이해하지 않으면 버전이 바뀔 때마다 무너진다는 걸 알았습니다."

---

## 2. 기술 선택 이유

### Q2-1. 왜 Spring Boot인가요? NestJS를 더 잘 다루신다면서요.

**답변**

> "정확히 **그 이유** 때문입니다. NestJS로 5개를 만들었는데 6번째도 NestJS면 포트폴리오의 정보량이 늘지 않습니다.
>
> 그리고 국내 백엔드 채용 시장에서 Spring/Java는 여전히 가장 큰 축입니다. **주력이 NestJS라는 건 유지하되, Spring 생태계를 읽고 쓸 수 있다는 걸 증명**하는 게 목표였습니다.
>
> 실제로 두 프레임워크를 비교하면서 배운 것도 있습니다. NestJS는 `@Module`에 provider를 명시적으로 등록하는데 Spring은 컴포넌트 스캔이 자동으로 합니다. 편하지만 **'왜 이 빈이 등록됐는지' 추적이 어려워집니다.** 자동 설정도 마찬가지고요. 그래서 이 프로젝트는 중요한 설정(`ddl-auto`, `open-in-view`, actuator `base-path`)을 **자동에 맡기지 않고 `application.yml`에 명시하고 이유를 주석으로** 남겼습니다."

**⚠️ 주의**: "NestJS보다 Spring이 좋아서"라고 하면 안 된다. 우열이 아니라 **커버리지와 상황**의 문제로 답한다.

---

### Q2-2. 왜 Spring Boot 3이 아니라 4인가요? 4는 아직 자료도 적을 텐데요.

**답변**

> "**Boot 3.5가 2026년 6월 30일 OSS 지원 종료(EOL)** 되기 때문입니다. 신규 포트폴리오 프로젝트를 EOL 버전으로 만드는 건 그 자체로 마이너스라고 판단했습니다.
>
> 자료가 적다는 게 리스크인 건 맞습니다. 그래서 **폴백 계획을 미리 세워뒀습니다** — 'C0 스캐폴딩 단계에서 30분 이상 막히면 Boot 3.5.16으로 내리고 springdoc도 2.8.x로 함께 내린다'고 계획서에 적어놨습니다. 하루짜리 프로젝트에서는 완성이 우선이니까요.
>
> 결과적으로 폴백 없이 4로 완주했고, **Boot 3 → 4 마이그레이션 포인트를 실제로 겪어본 경험**이 남았습니다. 자료가 적은 건 단점이지만, 곧 모든 팀이 겪을 마이그레이션을 먼저 해본 셈이라 오히려 이야깃거리가 됐습니다."

**꼬리질문: "구체적으로 뭐가 달랐나요?"**

> "네 가지가 실제로 저를 막았습니다.
> ① **Jackson 3** — 패키지 루트가 `com.fasterxml.jackson`에서 `tools.jackson`으로 바뀌어서, 습관대로 import 했다가 `ObjectMapper` 빈 주입이 안 됐습니다.
> ② **스타터 이름** — `spring-boot-starter-web`이 `spring-boot-starter-webmvc`로 바뀌었고, 테스트 스타터도 기능별로 쪼개졌습니다.
> ③ **springdoc** — 2.x가 Boot 4와 호환이 안 돼서 3.x 라인을 찾아야 했습니다.
> ④ **Security 7** — 이건 별도로 말씀드릴 게 좀 있습니다."

---

### Q2-3. Spring Security 7에서 뭐가 바뀌었나요?

**답변**

> "실제로 저를 막은 게 네 개입니다.
>
> **첫째, CSRF입니다.** stateless JWT API에서 `.csrf(disable)`을 명시적으로 하지 않으면 POST/PUT/PATCH/DELETE가 **전부 403**이 됩니다. 403이 뜨니까 권한 설정 문제인 줄 알고 `@PreAuthorize`랑 롤 접두사를 한참 뒤졌습니다. 여기서 얻은 진단 규칙이 **'GET은 되는데 쓰기만 403이면 CSRF부터 의심한다'** 입니다. 롤 문제라면 GET도 같이 막혔을 테니까요.
>
> **둘째, `authorizeRequests()`가 완전히 제거**됐습니다. `antMatchers()`, `mvcMatchers()`도요. 이제 `authorizeHttpRequests()` + `requestMatchers()`만 씁니다. 인터넷 예제가 컴파일조차 안 되는 주된 이유입니다.
>
> **셋째, `SessionCreationPolicy.STATELESS`를 명시**했습니다. JWT는 요청마다 토큰으로 신원을 증명하니 서버가 세션을 들 이유가 없고, 이게 수평 확장을 쉽게 만듭니다.
>
> **넷째, `@EnableMethodSecurity`** 입니다. 이게 없으면 `@PreAuthorize`가 **에러 없이 조용히 무시**됩니다. 개인적으로 이게 제일 무서운 함정이라고 생각합니다 — 권한 검사가 통째로 사라진 채 배포되니까요. 그래서 권한 매트릭스를 문서로만 두지 않고 **테스트로 잠갔습니다.**"

**⚠️ 정확성 함정 (반드시 읽을 것)**

면접관이 Spring을 잘 알면 이렇게 반박할 수 있다: **"CSRF는 Spring Security에서 원래부터 기본 ON이었는데요?"**

이때 이렇게 받는다:

> "맞습니다. `HttpSecurity`의 CSRF 기본값 자체는 예전부터 켜져 있었습니다. 제가 부딪힌 건 **Boot 4/Security 7 구성에서 stateless REST를 만들 때 이걸 명시적으로 꺼주지 않으면 쓰기 요청이 전부 막힌다**는 실제 증상이었고, Boot 4 마이그레이션 자료들도 '업그레이드 후 REST API가 403이 되는 가장 흔한 원인'으로 이걸 지목하고 있었습니다.
> 어느 쪽이든 제가 얻은 결론은 같습니다 — **stateless JWT API에서는 CSRF를 명시적으로 끄고, 왜 꺼도 되는지를 코드에 근거로 남긴다.** 실제로 주석에 '세션 쿠키를 쓰지 않으므로 CSRF가 성립하지 않는다'고 적어뒀습니다."

이렇게 답하면 **틀린 걸 인정하면서도 판단의 근거는 지켜내는** 그림이 된다. 우기면 감점, 이렇게 받으면 오히려 가점이다.

---

### Q2-4. JWT 라이브러리로 왜 jjwt를 안 쓰고 Spring 내장 Nimbus를 썼나요?

**답변**

> "**Jackson 3 충돌 때문**입니다.
>
> Spring Boot 4는 Jackson 3(`tools.jackson.*`)로 이행했는데, 가장 널리 쓰이는 `jjwt-jackson`은 Jackson 2(`com.fasterxml.jackson.*`) 바인딩입니다. 그래서 Boot 4에서 쓰려면 클래스패스에 Jackson 2를 되살리거나 `jjwt-gson`으로 우회해야 합니다. 어느 쪽이든 JSON 바인딩이 두 개가 되는 상황인데, 하루짜리 프로젝트에서 태울 시간이 아니라고 판단했습니다.
>
> 대신 `spring-boot-starter-security-oauth2-resource-server`를 쓰고 HS256 대칭키로 `NimbusJwtEncoder`/`NimbusJwtDecoder` 빈을 등록했습니다.
>
> 얻은 게 세 가지입니다.
> ① 의존성이 하나 줄고 JSON 바인딩 충돌이 없습니다.
> ② **커스텀 JWT 필터를 안 만들어도 됩니다** — `BearerTokenAuthenticationFilter`가 헤더 파싱, 서명 검증, 401 응답까지 다 합니다. 제가 설정한 건 'role 클레임을 `ROLE_` 권한으로 바꾸는 컨버터' 하나뿐입니다.
> ③ Spring Security의 인증 흐름과 자연스럽게 맞물려서, 401/403 처리도 `AuthenticationEntryPoint`/`AccessDeniedHandler`로 일관되게 붙습니다."

**꼬리질문: "왜 HS256인가요? RS256이 더 안전하지 않나요?"**

> "**발급자와 검증자가 같은 애플리케이션**이기 때문입니다. RSA 키쌍은 '내가 발급했다는 걸 제3자가 검증해야 할 때', 예를 들어 인증 서버와 리소스 서버가 분리된 MSA 구조에서 의미가 있습니다. 이 프로젝트는 한 앱이 발급하고 같은 앱이 검증하므로 **RSA 키쌍 관리는 과설계**입니다.
>
> 안전성 면에서 HS256이 RS256보다 약한 건 아닙니다. 다른 건 **키 배포 모델**이지 알고리즘 강도가 아닙니다. 다만 대칭키라서 **비밀 유지가 절대적**이라, `JWT_SECRET`을 환경변수로 분리하고 32바이트 미만이면 **애플리케이션 기동 자체를 실패**시키도록 `@PostConstruct`에서 검증했습니다. HS256의 최소 키 길이 요구를 런타임에 발견하는 게 아니라 부팅 시점에 잡으려고요."

**꼬리질문: "리프레시 토큰은요?"**

> "**없습니다.** 액세스 토큰 하나에 12시간 만료입니다. S티어 범위에서 의도적으로 뺐습니다.
>
> 리프레시 토큰을 제대로 하려면 **토큰 저장소, 회전(rotation), 재사용 감지, 폐기(revocation)** 까지 따라옵니다. 그걸 안 하고 리프레시 토큰만 추가하면 '만료가 긴 토큰이 하나 더 생긴' 것뿐이라 오히려 위험합니다.
>
> 대신 **12시간이라는 짧은 만료**로 노출 창을 줄였고, 프론트에서는 401을 받으면 즉시 로그아웃 처리하도록 했습니다. 참고로 리프레시 토큰 플로우는 다른 프로젝트(`newGym`)에서 access/refresh 하이브리드로 구현해봤습니다."

---

### Q2-5. 왜 Maven인가요? 요즘은 Gradle을 많이 쓰지 않나요?

**답변**

> "Spring Initializr 기본값이었고, XML이라 구조가 명시적이라는 점을 택했습니다. 팀에 새로 온 사람이 `pom.xml`을 열면 의존성이 뭔지 바로 읽힙니다.
>
> Gradle은 빌드 스크립트가 코드라서 훨씬 유연하고 빌드도 빠릅니다. 다만 **하루짜리 S티어 프로젝트에서 그 유연성이 필요한 지점이 없었습니다.**
>
> 정직하게 말씀드리면 **Gradle 실무 경험은 아직 없습니다.** 다음 Spring 프로젝트는 Gradle + Kotlin DSL로 해서 두 빌드 도구를 다 다뤄보려고 합니다."

**⚠️** 모르는 걸 아는 척하면 다음 질문에서 무너진다. **"아직 안 해봤고, 이렇게 채울 계획"** 이 신입에게 최선의 답이다.

---

### Q2-6. ORM으로 왜 JPA인가요? MyBatis나 JOOQ는요?

**답변**

> "세 가지 이유입니다.
>
> 첫째, **Spring 생태계의 표준**이고 채용 시장에서 가장 많이 요구합니다.
>
> 둘째, **다양성**입니다. 저는 Prisma와 TypeORM을 이미 다뤄봤습니다. JPA를 더하면 'ORM을 세 종류 써봤고 각각의 차이를 안다'가 됩니다. 실제로 JPA의 **영속성 컨텍스트와 더티 체킹**은 Prisma에 없는 개념이라 새로 배워야 했습니다.
>
> 셋째, 이 프로젝트는 **엔티티 중심 CRUD가 대부분**이라 JPA가 잘 맞습니다.
>
> 다만 JPA만 고집하지는 않았습니다. **멤버별 누적 벌금 집계는 네이티브 쿼리**로 썼습니다. `COUNT(*) FILTER (WHERE ...)` 같은 PostgreSQL 문법이 JPQL에 없고, 상태별 카운트 3개를 한 번에 뽑는 가장 짧은 방법이었기 때문입니다. **DB 종속성이 생긴다는 대가는 알고 받았습니다.**
>
> MyBatis나 JOOQ는 복잡한 조회 쿼리가 지배적인 시스템에서 강점이 있습니다. 이 프로젝트는 그런 성격이 아니었습니다."

---

## 3. 도메인 설계 — 벌금 스냅샷

### Q3-1. ★ `attendance_record.fine_amount`는 출석 상태와 현재 단가로 계산할 수 있는 값인데 왜 저장하나요? 비정규화 아닌가요?

> **이 프로젝트에서 가장 중요한 질문이다. 반드시 완벽하게 답한다.**

**답변 (3단 논리)**

> "**계산 가능한 값이지만 계산하면 안 되는 값**이라서 저장합니다.
>
> **① 저장하지 않으면 도메인 규칙이 깨집니다.**
> 조회할 때마다 현재 단가로 계산하면, 운영자가 지각 단가를 3,000원에서 5,000원으로 올리는 순간 **1월에 지각한 사람의 벌금도 5,000원으로 소급 인상**됩니다. 그 사람은 3,000원 내기로 하고 지각한 겁니다. 이건 버그가 아니라 **약속 위반**입니다.
>
> **② 이건 중복이 아니라 서로 다른 두 개념입니다.**
> 정규화가 막으려는 건 '같은 사실이 두 곳에 있어서 한쪽만 고쳐지는 갱신 이상'입니다. 그런데 `study_room.late_fine_amount`와 `attendance_record.fine_amount`는 **같은 사실이 아닙니다.**
> 전자는 '**현재** 설정된 단가(FineRate)', 후자는 '**그 시점에 확정된 부과액**(Fine)'입니다. 시간축이 다릅니다. 그래서 용어집에서 아예 **이름부터 FineRate와 Fine으로 분리**했습니다. 이름이 같으면 개발자가 헷갈리고, 헷갈리면 코드가 틀립니다.
>
> **③ 실무의 표준 패턴입니다.**
> 주문 라인아이템에 주문 당시 상품 가격을 박아두는 것과 정확히 같습니다. 상품 가격이 올랐다고 작년 영수증 금액이 바뀌면 회계 사고입니다. 계약서, 급여명세서, 세금계산서 전부 같은 패턴입니다."

**꼬리질문 A: "그럼 벌금 단가 이력 테이블(rate history)을 만들면 더 정규화된 설계 아닌가요?"**

> "가능한 대안이고 실제로 고민했습니다. `fine_rate_history(effective_from, late_amount, absent_amount)`를 두고, 조회할 때 회차 날짜에 맞는 단가를 찾아 조인하는 방식입니다.
>
> **얻는 것**: 단가 변경 이력 자체를 조회할 수 있습니다.
> **잃는 것**: 모든 벌금 조회에 시간 범위 조인이 붙고, 경계 조건(같은 날 두 번 바꾸면?)을 다뤄야 합니다.
>
> **'단가 변경 이력 조회'가 요구사항이 아니었기 때문에** 스냅샷 하나로 충분하다고 판단했습니다. 그리고 중요한 건, 나중에 그게 요구사항이 되면 **이력 테이블을 추가만 하면 되고 기존 `fine_amount`는 그대로 둬도 됩니다.** 확장 경로가 막히지 않는다는 뜻입니다."

**꼬리질문 B: "실수로 소급 계산하는 코드가 들어가면요?"**

> "그걸 막으려고 **세 겹**으로 했습니다.
>
> ① **의존 방향을 잘랐습니다.** `StudyRoomService`(단가 변경)는 `AttendanceRepository`나 `AttendanceRecord`를 **import조차 하지 않습니다.** 단가 변경 코드가 출석 기록에 닿을 경로가 물리적으로 없습니다.
> ② **변경 통로를 하나로 좁혔습니다.** `fineAmount`를 바꾸는 유일한 메서드는 `AttendanceRecord.updateStatus(status, fineAmount)`이고, 호출부는 `AttendanceService.checkIn` 한 곳뿐입니다.
> ③ **테스트로 잠갔습니다.** `FinePolicyTest.snapshot_pastResultDoesNotChangeWhenRateChangesLater`가 '계산 결과를 받아둔 뒤 단가를 바꿔도 이전 값이 안 변한다'를 검증합니다.
>
> 원칙을 문서에만 적어두면 6개월 뒤에 누가 깹니다. **구조로 막고 테스트로 고정**하는 게 맞다고 봤습니다."

**꼬리질문 C: "그럼 단가를 잘못 입력해서 과거 기록을 고쳐야 하면요?"**

> "**의도적으로 API로는 불가능하게 뒀습니다.** 대신 그 회차의 출석 체크를 다시 저장하면 현재 단가로 재확정됩니다 — 운영자가 '이 회차를 다시 정산한다'는 명시적 행동을 해야 하는 겁니다.
>
> 실무라면 여기에 **감사 로그(누가 언제 왜 재정산했는지)** 를 붙이는 게 맞다고 생각합니다. 돈과 관련된 값이 조용히 바뀌면 안 되니까요. S티어 범위 밖이라 넣지 않았지만, 이 프로젝트를 확장한다면 벌금 납부 관리와 함께 우선순위 높게 넣을 항목입니다."

---

### Q3-2. 누적 벌금은 왜 컬럼으로 저장하지 않나요? 매번 집계하면 느리지 않나요?

**답변**

> "**진실을 두 개 만들지 않기 위해서**입니다.
>
> `member.total_fine` 같은 합계 컬럼을 두면 출석 기록이 바뀔 때마다 두 곳을 동기화해야 합니다. 출석 체크 저장, 회차 삭제(CASCADE), 멤버 비활성화 — 이 모든 경로에서 합계를 맞춰야 하는데, **이런 보정 로직은 반드시 어딘가에서 빠집니다.** 그리고 어긋나면 어느 쪽이 진짜인지 알 수 없게 됩니다.
>
> 성능은 규모를 보고 판단했습니다. **멤버 수십 명 × 회차 수십 개 = 수천 행**입니다. `LEFT JOIN + GROUP BY` 한 번이면 밀리초 단위입니다. 게다가 `attendance_record.member_id`에 인덱스가 있어서 집계 경로도 확보돼 있습니다.
>
> 성능이 문제되는 규모(수십만 행)가 오면 **그때 구체화 뷰나 집계 테이블을 얹는 게 순서**입니다. 지금 넣는 건 과설계입니다.
>
> 부수 효과도 있습니다. 회차를 삭제하면 그 회차분 벌금이 **자연히 누적에서 빠집니다.** 보정 로직이 아예 필요 없습니다."

**꼬리질문: "규모가 커지면 어떻게 하실 건가요? 구체적으로요."**

> "단계별로 봅니다.
> **1단계**: 인덱스 확인 + `EXPLAIN ANALYZE`로 실행 계획 점검. 대부분 여기서 해결됩니다.
> **2단계**: 멤버 목록에 **페이지네이션**을 넣습니다. 지금은 전체를 한 번에 주는데, 그 자체가 먼저 병목이 됩니다.
> **3단계**: 여전히 느리면 **구체화 뷰(materialized view)** 를 만들고 주기적으로 갱신합니다. 이건 '합계 컬럼'과 달리 **갱신 책임이 한 곳(리프레시 잡)에 모여** 있어서 이중 갱신 버그가 안 생깁니다.
> **4단계**: 실시간성이 꼭 필요하면 그때 이벤트 기반 증분 갱신을 고민합니다.
>
> 핵심은 **1~~2단계로 해결될 문제에 3~~4단계를 미리 넣지 않는 것**입니다."

---

### Q3-3. `FinePolicy`를 왜 스프링 빈이 아니라 static 유틸로 만들었나요?

**답변**

> "**DB도 스프링도 모르는 순수 함수로 두고 싶었기 때문**입니다.
>
> ```java
> public static int calculate(AttendanceStatus status, StudyRoom room) {
>     return switch (status) {
>         case PRESENT -> 0;
>         case LATE    -> room.getLateFineAmount();
>         case ABSENT  -> room.getAbsentFineAmount();
>     };
> }
> ```
>
> 이렇게 두면 **테스트에 목(mock)도, 스프링 컨텍스트도 필요 없습니다.** 실행이 밀리초 단위라 케이스를 많이 쓸 수 있고, 실제로 벌금 계산 테스트가 전체 28개 중 상당수를 차지합니다. **S티어 하루 프로젝트에서 핵심 도메인 규칙의 커버리지를 확보할 수 있었던 이유**가 이 설계입니다.
>
> 그리고 `switch` **표현식**을 쓴 것도 의도적입니다. enum에 대한 switch 표현식은 컴파일러가 **빠짐없음(exhaustiveness)을 검사**합니다. 나중에 `EXCUSED`(사유 결석) 같은 상태를 추가하면 **이 코드가 컴파일 에러**를 냅니다. 런타임에 조용히 0원이 되는 것보다 빌드가 깨지는 게 낫습니다."

**꼬리질문: "static은 테스트하기 어렵다고들 하는데요?"**

> "**의존성이 있는 static**이 어려운 겁니다. `FinePolicy.calculate`는 인자로 받은 것만 쓰고 부작용이 없어서 오히려 가장 테스트하기 쉬운 형태입니다.
>
> 반대로 이걸 `@Component`로 만들었다면 서비스 테스트에서 **목으로 대체할 수 있게** 됐을 텐데, 그건 오히려 위험합니다. 벌금 계산 규칙을 목으로 바꿔치기해서 테스트하면 **실제 계산 로직이 테스트 대상에서 빠집니다.** 이 도메인에서 벌금 계산은 대체 가능한 협력자가 아니라 **불변의 규칙**이라 static이 의미상으로도 맞습니다."

---

### Q3-4. 출석 체크를 왜 PUT으로 했나요? POST가 아니라?

**답변**

> "**멱등성** 때문입니다.
>
> `PUT /api/sessions/{id}/attendances`는 '이 회차의 출석 현황을 이 상태로 만들어라'라는 의미입니다. 같은 요청을 두 번 보내도 결과가 같습니다. 네트워크가 끊겨서 사용자가 저장 버튼을 다시 눌러도 중복 기록이 생기지 않습니다.
>
> POST였다면 '출석 기록을 새로 만들어라'가 되어 두 번 누르면 두 벌이 생기는 게 자연스러운 의미가 됩니다. 실제로는 유니크 제약 때문에 실패하겠지만, **HTTP 메서드의 의미와 실제 동작이 어긋나는 건 좋은 API가 아닙니다.**
>
> 그리고 이 프로젝트의 출석 체크는 애초에 **회차 단위 일괄 저장**입니다. '멤버 한 명 추가'가 아니라 '이 회차 전원의 상태를 이렇게 확정'이라서 PUT의 전체 교체 의미와 잘 맞습니다."

**꼬리질문: "정말 멱등인가요? 벌금 단가가 중간에 바뀌면 두 번째 저장은 다른 결과가 나올 텐데요?"**

> "**날카로운 지적이고, 맞습니다.** 엄밀히 말하면 '외부 상태(벌금 단가)가 그대로일 때 멱등'입니다.
>
> 다만 이건 **의도된 동작**이기도 합니다. 단가를 바꾼 뒤 같은 회차를 다시 저장하는 건 '이 회차를 새 단가로 재정산한다'는 명시적 행위니까요. 스냅샷 원칙과도 일관됩니다 — **저장하는 순간의 단가로 확정**한다는 규칙이 두 번째 호출에도 똑같이 적용되는 것뿐입니다.
>
> 실무라면 이런 재정산이 감사 로그에 남아야 한다고 생각합니다."

---

### Q3-5. 왜 회차(StudySession)를 별도 테이블로 뒀나요? 출석 기록에 날짜를 두면 안 되나요?

**답변**

> "기획 단계에서 실제로 그 안이 나왔습니다. 출석 기록이 `session_date`를 직접 들고 `UNIQUE(member_id, session_date)`를 거는 방식이요. **세 가지 문제로 접었습니다.**
>
> ① 회차 목록을 뽑으려면 `SELECT DISTINCT session_date FROM attendance_record`를 해야 합니다.
> ② **출석 기록이 하나도 없는 회차는 존재 자체가 표현되지 않습니다.** '만들어는 뒀는데 아직 체크 안 한 회차'가 사라집니다. 운영자 입장에서는 이게 가장 필요한 정보인데요.
> ③ 회차 제목 같은 속성을 걸 곳이 없습니다.
>
> 회차는 그 자체로 **1급 도메인 개념**입니다. 테이블 하나 + 단순 CRUD 비용으로 위 문제가 전부 사라지므로 분리했습니다. 유니크 제약도 `(session_id, member_id)`로 옮겼습니다.
>
> 그리고 `session_date`를 `timestamptz`가 아니라 **`date` 타입**으로 뒀습니다. 모임은 '그 날'의 일이지 시각의 일이 아닙니다. `timestamptz`로 두면 타임존 때문에 **날짜가 하루 밀리는 고전 버그**가 생깁니다."

---

## 4. JPA / 데이터베이스

### Q4-1. ★ N+1 문제가 뭔지 설명하고, 이 프로젝트에서 어떻게 막았는지 말씀해주세요.

**답변**

> "**N+1은 목록 조회 1번 + 항목마다 연관 조회 N번, 총 1+N번의 쿼리가 나가는 문제**입니다.
>
> 예를 들어 회차 상세에서 출석 기록 4건을 가져온 뒤 각각 `record.getMember().getName()`을 호출하면, `@ManyToOne`이 LAZY라서 **멤버 SELECT가 4번 더** 나갑니다. 출석이 100건이면 쿼리 101번입니다.
>
> 이 프로젝트에서는 **지점마다 다른 방식**으로 막았습니다.
>
> **① GROUP BY 프로젝션 (멤버 목록 + 누적 벌금)**
>
> ```sql
> SELECT m.id, m.name, ...,
>        COALESCE(SUM(ar.fine_amount), 0)                 AS accumulatedFine,
>        COUNT(ar.id) FILTER (WHERE ar.status = 'LATE')   AS lateCount
> FROM member m
> LEFT JOIN attendance_record ar ON ar.member_id = m.id
> GROUP BY m.id
> ```
>
> 엔티티가 아니라 **인터페이스 프로젝션**으로 받습니다. 엔티티로 받아서 자바에서 합계를 내면 그게 N+1입니다. `LEFT JOIN`이라 출석 기록이 없는 신규 멤버도 0원으로 나옵니다. 회차 목록의 벌금 합계도 같은 패턴입니다.
>
> **② `@EntityGraph` / `join fetch` (연관 엔티티가 실제로 필요할 때)**
>
> ```java
> @EntityGraph(attributePaths = "member")
> List<AttendanceRecord> findByStudySessionId(Long studySessionId);
> ```
>
> 내 출석 내역은 정렬까지 필요해서 `join fetch ar.studySession s ... order by s.sessionDate desc`로 JPQL을 직접 썼습니다.
>
> **③ 사전 조회 + Map (일괄 저장)**
> 출석 체크는 멤버 루프 안에서 리포지토리를 부르면 바로 N+1입니다. 그래서 루프 **진입 전에** ⓐ 멤버 존재 검증 1회(`findAllById`), ⓑ 스터디룸 단가 1회, ⓒ 기존 기록 1회(`Map<memberId, record>`)를 끝내고, **루프 안에는 리포지토리 호출이 0건**입니다. 순수 계산과 `saveAll` 1회뿐입니다.
>
> **④ `open-in-view: false`**
> 전역 안전장치입니다. 기본값 `true`면 뷰 렌더 단계까지 영속성 컨텍스트가 열려 있어서 **지연 로딩이 '그냥 동작'해버려 N+1을 숨깁니다.** 꺼두면 서비스 레이어 밖에서 LAZY 필드를 만지는 순간 예외가 나서 즉시 알려줍니다."

**꼬리질문: "N+1이 없다는 걸 어떻게 확인하셨나요?"**

> "`spring.jpa.properties.hibernate.generate_statistics: true`를 local 프로파일에 켜고 실행 쿼리 수를 로그로 확인했습니다. 멤버 수를 늘려도 쿼리 수가 그대로인 걸 눈으로 봤습니다.
>
> 다만 **정직하게 말씀드리면, 자동화된 쿼리 카운트 테스트는 없습니다.** 실제 DB가 필요해서 Testcontainers가 있어야 하는데, S티어 범위에서 뺐습니다. 지금은 리팩터하다가 누가 `@EntityGraph`를 지워도 CI가 안 잡아줍니다.
>
> 다른 프로젝트(`match-mate`)에서는 Testcontainers로 **6명일 때와 18명일 때 쿼리 수가 똑같이 10개**인 걸 어서션으로 고정했습니다. 이 프로젝트에도 그 방식을 적용하는 게 다음 개선 항목입니다."

**⚠️** 이 답변의 마지막 문단이 중요하다. **"했다"만 말하면 얕고, "여기까지 했고 여기부터는 못 했고 이렇게 채울 것"이 깊다.**

---

### Q4-2. `existing.updateStatus()`만 호출하고 `save()`를 안 하는데 어떻게 DB에 반영되나요?

**답변**

> "**더티 체킹(dirty checking)** 때문입니다.
>
> `@Transactional` 메서드 안에서 리포지토리로 조회한 엔티티는 **영속성 컨텍스트가 관리하는 상태(managed)** 가 됩니다. Hibernate가 조회 시점의 스냅샷을 들고 있다가, 트랜잭션 커밋(또는 flush) 시점에 현재 값과 비교해서 **달라진 필드만 UPDATE 문**으로 만듭니다.
>
> 그래서 코드가 이렇게 됩니다.
>
> ```java
> if (existing != null) {
>     existing.updateStatus(item.status(), fineAmount);   // 조회해온 엔티티 → 더티 체킹
> } else {
>     newRecords.add(new AttendanceRecord(...));          // new 한 객체 → 비영속(transient)
> }
> attendanceRepository.saveAll(newRecords);               // 신규만 명시적 저장
> ```
>
> 새로 `new`한 객체는 영속성 컨텍스트가 모르는 비영속 상태라 **명시적으로 `save()` 해줘야** 합니다. 그래서 신규만 모아서 `saveAll`을 부릅니다."

**꼬리질문: "저장 직후에 `studySessionService.detail()`로 재조회하는데, 아직 커밋 전이잖아요. 최신 값이 보이나요?"**

> "네, 보입니다. **JPQL이나 네이티브 쿼리를 실행하기 직전에 Hibernate가 자동 flush를 합니다.** 아직 DB에 안 보낸 변경이 있으면 쿼리 결과가 틀리게 나오니까요. 그래서 같은 트랜잭션 안에서 재조회하면 방금 바꾼 값이 반영돼 있습니다.
>
> 그리고 `detail()`이 `@Transactional(readOnly = true)`인데, 이건 **바깥 트랜잭션에 참여(REQUIRED)** 하므로 readOnly 힌트는 적용되지 않습니다. 새 트랜잭션이 열렸다면 flush 모드 때문에 문제가 될 수 있는 구조인데, 참여하기 때문에 정상 동작합니다."

**⚠️** 이 마지막 부분까지 말하면 "트랜잭션 전파를 이해하고 있다"는 신호가 된다. 자신 없으면 앞부분만 말하고 멈춘다.

---

### Q4-3. `open-in-view`를 왜 껐나요? 불편하지 않나요?

**답변**

> "**불편한 게 목적**입니다.
>
> 이유가 두 가지입니다.
>
> ① **커넥션 낭비.** OSIV가 켜져 있으면 컨트롤러가 JSON을 만드는 동안에도 DB 커넥션을 붙들고 있습니다. 트래픽이 늘면 커넥션 풀이 먼저 마릅니다.
>
> ② **N+1을 숨깁니다.** 이게 더 중요합니다. OSIV가 켜져 있으면 컨트롤러나 직렬화 단계에서 지연 로딩이 '그냥 동작'해버립니다. 개발 중에는 문제를 못 느끼다가 트래픽이 오르면 터집니다. 꺼두면 서비스 레이어 밖에서 LAZY 필드를 만지는 순간 `LazyInitializationException`이 나서 **개발 시점에 즉시 알려줍니다.**
>
> 불편함은 **필요한 데이터를 서비스 레이어에서 명시적으로 fetch join하거나 DTO로 변환하도록 강제**하는 형태로 나타나는데, 그게 정확히 제가 원한 강제입니다. 실제로 이 프로젝트는 응답을 전부 record DTO로 만들어서 엔티티가 컨트롤러 밖으로 안 나갑니다."

---

### Q4-4. 인덱스를 어떻게 설계하셨나요?

**답변**

> "네 개만 만들었습니다. **인덱스는 공짜가 아니라서**(쓰기 비용 + 용량) 근거가 있는 것만 뒀습니다.
>
> | 인덱스                                     | 종류   | 근거                                                          |
> | ------------------------------------------ | ------ | ------------------------------------------------------------- |
> | `member(email)`                            | UNIQUE | 로그인마다 조회 + 중복 가입 차단. 제약과 인덱스를 동시에 해결 |
> | `study_session(session_date)`              | UNIQUE | 같은 날 회차 중복 차단 + 날짜 정렬 조회                       |
> | `attendance_record(session_id, member_id)` | UNIQUE | **한 회차에 한 멤버 1건**. 일괄 저장 중복 방지의 최종 방어선  |
> | `attendance_record(member_id)`             | 일반   | 누적 벌금 집계 / 내 출석 내역의 핵심 경로                     |
>
> **마지막 게 가장 설명할 게 많은 인덱스**입니다."

**꼬리질문 (거의 반드시 나온다): "복합 유니크에 `member_id`가 이미 있는데 왜 또 만드나요?"**

> "**복합 인덱스는 선두 컬럼(leftmost prefix)부터 써야 유효**하기 때문입니다.
>
> `(session_id, member_id)` 인덱스는 전화번호부가 (성, 이름) 순으로 정렬된 것과 같습니다. '김'으로 시작하는 사람은 빨리 찾지만, **'영선'이라는 이름만으로는** 처음부터 끝까지 훑어야 합니다. 마찬가지로 `WHERE member_id = ?`는 이 인덱스를 못 타고 풀스캔합니다.
>
> `GET /api/me/attendances`와 멤버별 누적 벌금 집계가 정확히 `member_id`로만 필터링하므로 단독 인덱스가 필요합니다.
>
> 그리고 하나 더 중요한 게 있는데, **PostgreSQL은 FK를 걸어도 인덱스를 자동 생성하지 않습니다.** 참조되는 쪽 PK에는 있지만 참조하는 쪽 컬럼에는 없습니다. MySQL InnoDB는 자동으로 만들어줘서 'FK니까 인덱스 있겠지' 하고 착각하기 쉬운데, Postgres에서는 틀립니다."

**꼬리질문: "만들지 않은 인덱스는요?"**

> "세 가지를 의도적으로 뺐습니다.
> ① `attendance_record.session_id` 단독 — 복합 유니크의 **선두 컬럼**이라 이미 커버됩니다. 만들면 중복입니다.
> ② `member.role`, `member.active` — **카디널리티가 2~3**입니다. 값 종류가 몇 개 안 되면 인덱스를 타도 대부분의 행을 읽게 되어, 수십 행 테이블에서는 오히려 풀스캔이 빠릅니다.
> ③ `attendance_record.status` — 위와 같고, 집계는 어차피 전체 스캔입니다.
>
> **'인덱스를 안 만든 이유'를 설명할 수 있는 게 만든 이유를 설명하는 것만큼 중요**하다고 생각합니다."

---

### Q4-5. Flyway를 쓰셨는데, `ddl-auto`를 왜 `validate`로 했나요?

**답변**

> "**스키마 변경은 오직 Flyway로만** 한다는 원칙 때문입니다.
>
> `update`를 쓰면 두 가지가 위험합니다.
> ① Hibernate가 **예고 없이 스키마를 건드립니다.** 엔티티를 고치면 운영 DB의 테이블이 자동으로 바뀝니다.
> ② 더 나쁜 건, **컬럼 삭제나 타입 변경을 조용히 무시**한다는 겁니다. 즉 '적용된 줄 알았는데 안 된' 상태가 생깁니다.
>
> `validate`는 엔티티와 실제 스키마를 **대조만** 합니다. 어긋나면 **애플리케이션 기동 시점에 즉시 실패**합니다. 런타임에 이상한 SQL 에러로 만나는 것보다 훨씬 낫습니다.
>
> Flyway 규칙도 두 가지 지켰습니다.
> ① **적용된 마이그레이션 파일은 절대 수정하지 않습니다.** Flyway가 체크섬을 저장해뒀다가 비교하는데, 한 글자만 고쳐도 기동이 막힙니다. 변경이 필요하면 새 버전 파일을 추가합니다.
> ② **Boot 4에서는 `flyway-database-postgresql` 아티팩트를 반드시 함께 넣어야** 합니다. Flyway 10부터 DB별 모듈이 분리돼서, 누락하면 기동 시 `Unsupported Database: PostgreSQL`로 실패합니다. 실제로 이걸로 한 번 막혔습니다."

**꼬리질문: "그럼 데모 시드 데이터도 Flyway로 넣으셨나요?"**

> "**나눴습니다.** 기준은 '**운영에서도 필요한가**'입니다.
>
> `study_room` id=1 행은 **`V1__init.sql`에 넣었습니다.** 앱이 뜨려면 항상 있어야 하는 설정 행이라 데이터가 아니라 **스키마의 일부**로 취급했습니다.
>
> admin 계정과 샘플 데이터는 **`DemoDataSeeder`(`ApplicationRunner`)** 로 넣었습니다. 이유가 세 개입니다.
> ① BCrypt 해시를 SQL 리터럴로 박으면 강도(strength)를 바꿀 때 손댈 수 없고, 해시를 소스에 커밋하는 것도 좋지 않습니다.
> ② `APP_SEED_ENABLED=false`로 **끌 수 있어야** 합니다. Flyway 마이그레이션은 못 끕니다.
> ③ 멱등성 — `existsByEmail("admin")`이면 통째로 return합니다. 재기동해도 중복 데이터가 안 생깁니다."

---

### Q4-6. enum을 어떻게 저장하셨나요?

**답변**

> "**`@Enumerated(EnumType.STRING)` + DB의 `CHECK` 제약**입니다.
>
> ```sql
> role VARCHAR(20) NOT NULL CHECK (role IN ('ORGANIZER', 'MEMBER'))
> ```
>
> `ORDINAL`은 **절대 안 씁니다.** enum 상수의 **순서만 바꿔도** 기존 데이터의 의미가 통째로 뒤집힙니다. `MEMBER=0, ORGANIZER=1`이었는데 누가 순서를 바꾸면 **모든 일반 멤버가 운영자가 됩니다.** 코드 리뷰에서 잡기도 어렵습니다 — enum 선언 한 줄만 바뀌니까요.
>
> PostgreSQL 네이티브 `ENUM` 타입도 고민했는데, 값 추가/삭제 마이그레이션이 번거로워서 뺐습니다. **`VARCHAR` + `CHECK`가 가독성(DB를 직접 열어봐도 읽힙니다)과 변경 용이성의 균형점**이라고 봤습니다."

---

### Q4-7. 금액을 왜 `integer`로 하셨나요? `BigDecimal`이 정석 아닌가요?

**답변**

> "**통화가 KRW 하나로 고정**이고 원화는 소수점이 없기 때문입니다.
>
> `BigDecimal`은 소수점 계산의 정밀도 문제를 해결하려고 쓰는 건데, 원화 벌금은 천 원 단위 정수입니다. `integer` 최대값이 21억 원이라 스터디 벌금에는 차고 넘칩니다. 통화 컬럼도 안 뒀습니다.
>
> 다만 **함정이 하나 있었습니다.** `SUM(integer)`의 결과는 PostgreSQL에서 **`bigint`** 입니다. Java 프로젝션 타입을 `int`로 잡으면 매핑 예외가 납니다. 그래서 **누적 합계는 `long`으로** 받도록 했습니다.
>
> 만약 다중 통화나 환율이 들어오면 그땐 `BigDecimal` + 통화 코드 + 소수 자릿수 정책을 다시 설계해야 합니다. 지금 넣는 건 과설계입니다."

---

## 5. 보안 / 인증 / 인가

### Q5-1. 인증 흐름을 설명해주세요.

**답변**

> "**stateless JWT** 방식입니다.
>
> **로그인**: `POST /api/auth/login`으로 이메일/비밀번호를 받고, `BCryptPasswordEncoder.matches()`로 검증한 뒤 `NimbusJwtEncoder`로 토큰을 발급합니다. 클레임은 `sub`(memberId), `role`, `name`, `exp`(12시간)입니다.
>
> **보호된 요청**: `Authorization: Bearer <token>` 헤더로 옵니다. Spring Security의 `BearerTokenAuthenticationFilter`가 헤더를 파싱하고 `NimbusJwtDecoder`로 서명과 만료를 검증합니다. **실패하면 컨트롤러에 도달하지도 않고 401**입니다.
>
> 성공하면 `JwtAuthenticationConverter`가 `role` 클레임을 `ROLE_ORGANIZER` 같은 권한으로 변환해서 `SecurityContext`에 넣습니다. 그 다음 컨트롤러의 `@PreAuthorize("hasRole('ORGANIZER')")`가 권한을 확인하고, 부족하면 403입니다.
>
> **커스텀 JWT 필터를 안 만든 게 포인트**입니다. 스프링이 제공하는 필터가 이미 다 하고, 제가 설정한 건 컨버터 하나뿐입니다."

**꼬리질문: "필터 단계 401과 컨트롤러 단계 예외의 응답 포맷은 어떻게 통일하셨나요?"**

> "**좋은 지적입니다. 실제로 신경 쓴 부분입니다.**
>
> 필터 체인에서 막힌 401/403은 `DispatcherServlet`에 도달하지 않아서 `@RestControllerAdvice`가 **못 잡습니다.** 그래서 그냥 두면 필터 단계는 스프링 기본 응답, 컨트롤러 단계는 `ProblemDetail`로 **포맷이 두 가지**가 됩니다. 프론트가 두 가지를 다 파싱해야 하죠.
>
> 그래서 `SecurityConfig`에 `AuthenticationEntryPoint`와 `AccessDeniedHandler`를 직접 등록해서 **같은 `ProblemDetail` 포맷으로 응답을 씁니다.** 문구도 `ProblemTitles`라는 공용 클래스에 모아서, 두 경로가 다른 문구를 뱉지 않게 했습니다."

---

### Q5-2. ★ CSRF를 껐는데 보안상 괜찮나요?

> **이 질문은 반드시 나온다.**

**답변**

> "**이 API 구조에서는 CSRF가 성립하지 않기 때문에** 괜찮습니다. 논리를 정확히 말씀드리면요.
>
> CSRF는 **브라우저가 인증 정보를 자동으로 실어 보낼 때** 성립하는 공격입니다. 공격자 사이트에서 폼을 제출하면 브라우저가 우리 도메인의 **세션 쿠키를 자동으로 붙여서** 보내니까 서버가 정상 요청으로 착각하는 거죠.
>
> 이 API는 **세션 쿠키를 전혀 쓰지 않습니다.** `SessionCreationPolicy.STATELESS`로 세션을 만들지도 않고, 인증은 오직 `Authorization: Bearer` 헤더로만 합니다. **헤더는 브라우저가 자동으로 붙여주지 않습니다** — 자바스크립트가 명시적으로 넣어야 합니다. 그리고 공격자 사이트의 스크립트는 동일 출처 정책 때문에 우리 도메인의 `localStorage`에 접근할 수 없습니다.
>
> 즉 공격자가 위조 요청을 만들어도 **토큰을 실을 방법이 없어서** 401로 거부됩니다.
>
> 반대로, **만약 토큰을 httpOnly 쿠키로 옮긴다면 CSRF를 반드시 다시 켜야 합니다.** 그 순간 브라우저가 자동으로 쿠키를 붙이기 시작하니까요. 코드 주석에도 이 근거를 적어뒀습니다."

**꼬리질문: "그럼 XSS는요? localStorage는 XSS에 노출되잖아요."**

> "**맞습니다. 그게 이 선택의 실제 대가입니다.** README의 '알려진 트레이드오프'에 명시했습니다.
>
> |               | localStorage               | httpOnly 쿠키                              |
> | ------------- | -------------------------- | ------------------------------------------ |
> | XSS           | ❌ 스크립트가 읽을 수 있음 | ✅ 스크립트 접근 불가                      |
> | CSRF          | ✅ 자동 전송 안 됨         | ❌ 대응 필요                               |
> | 크로스 오리진 | ✅ 헤더라 간단             | ❌ SameSite·도메인·`credentials` 설정 필요 |
>
> httpOnly 쿠키가 XSS 관점에서는 정석입니다. 다만 이 프로젝트는 **프론트가 Vercel, 백엔드가 Render로 도메인이 다릅니다.** 크로스 사이트 쿠키를 쓰려면 `SameSite=None; Secure` + `allowCredentials` + CSRF 토큰까지 세트로 붙어야 하고, 그러면 CSRF 대응이 다시 필요해집니다.
>
> S티어 범위에서 **localStorage + 짧은 만료(12시간)** 로 절충하고, **이 선택을 알고 있는 상태로 문서에 남겼습니다.** 실서비스라면 XSS 자체를 막는 게 우선이라 CSP 헤더와 입력 이스케이프를 먼저 붙이고, 그 다음 httpOnly 쿠키 + CSRF 토큰 조합으로 갈 것 같습니다."

**⚠️** "괜찮습니다"로만 끝내면 안 된다. **대가를 정확히 말하는 게 핵심**이다.

---

### Q5-3. ★ 멤버가 다른 사람 벌금을 보지 못하게 어떻게 막았나요?

**답변**

> "**비교문이 아니라 URL 구조로** 막았습니다. 이게 이 프로젝트에서 제일 애착이 가는 설계입니다.
>
> 흔한 방식은 이렇습니다.
>
> ```java
> // ⛔ 이 방식은 쓰지 않았습니다
> if (!isOrganizer(auth) && !id.equals(currentMemberId)) throw new AccessDeniedException();
> ```
>
> 문제는 **엔드포인트가 늘어날 때마다 이 비교를 반복**해야 하고, **한 곳에서 빠뜨리면 바로 정보 유출**이라는 겁니다. 코드 리뷰로 매번 잡아야 합니다.
>
> 그래서 경로를 두 개로 나눴습니다.
>
> | 용도      | 경로                                | 권한        | id 파라미터                |
> | --------- | ----------------------------------- | ----------- | -------------------------- |
> | 본인 조회 | `GET /api/me/attendances`           | 로그인만    | **없음** (토큰 `sub` 사용) |
> | 타인 조회 | `GET /api/members/{id}/attendances` | ORGANIZER만 | 있음                       |
>
> 본인 조회는 **클라이언트가 id를 보낼 문법이 URL에 아예 없습니다.** 남의 데이터를 지칭할 방법이 존재하지 않습니다. 그리고 id를 받는 경로는 **예외 없이 ORGANIZER 전용**이라, MEMBER는 `{id}`에 자기 id를 넣어도 403입니다.
>
> 이렇게 하면 나중에 이 컨트롤러에 엔드포인트를 추가하는 사람이 소유권 비교를 잊어도 **취약점이 생기지 않습니다.** 실수할 수 있는 자리 자체를 없앤 겁니다."

**꼬리질문: "자기 id인데 403이면 사용자 입장에서 이상하지 않나요?"**

> "**기능 손실은 없습니다.** 본인 조회는 `/api/me/attendances`라는 전용 경로가 있으니까요. 프론트도 그 경로만 씁니다.
>
> 오히려 '자기 id면 통과'를 허용하는 순간, 그 판정 로직을 **모든 엔드포인트에서 정확히 반복**해야 합니다. 저는 '조금 덜 유연하지만 실수할 수 없는 API'가 이 경우엔 낫다고 판단했습니다.
>
> 보안 설계 원칙 중에 **'안전한 기본값(secure by default)'** 과 **'실수하기 어려운 API'** 라는 게 있는데, 그 사례라고 생각합니다."

**꼬리질문: "프론트에서도 MEMBER는 운영자 메뉴가 안 보이던데, 그럼 이중 방어인가요?"**

> "**아니요, 프론트 가드는 보안 경계가 아닙니다.** UX일 뿐입니다.
>
> 브라우저 자바스크립트는 사용자가 얼마든지 조작할 수 있습니다. `RequireOrganizer` 컴포넌트를 우회하거나, 아예 콘솔에서 `fetch`로 API를 직접 부르면 그만입니다. **실제 방어선은 서버의 `@PreAuthorize`** 하나뿐입니다.
>
> 그래서 `RequireOrganizer.tsx` 주석에도 '이건 UX 편의일 뿐이고 실제 보안 경계는 서버'라고 명시해뒀습니다. 프론트 가드를 보안으로 착각하는 게 신입이 자주 하는 실수라고 들어서, 착각하지 않았다는 걸 코드에 남겨두고 싶었습니다."

---

### Q5-4. ★ 데모 계정 `admin`/`admin`은 보안 구멍 아닌가요?

> **면접관이 가장 의심스럽게 볼 부분이다. 정확한 방어 논리가 필요하다.**

**답변**

> "**우려하시는 지점을 정확히 알고 설계했습니다.** 세 가지 원칙을 지켰습니다.
>
> **① 비밀번호 검증을 절대 우회하지 않습니다.**
> `AuthService.login`에 admin에 대한 `if` 분기가 **하나도 없습니다.** `passwordEncoder.matches()`를 무조건 통과해야 하고, 시드도 `passwordEncoder.encode("admin")`으로 **진짜 BCrypt 해시**를 저장합니다. admin이라고 특별 취급하는 코드 경로가 없습니다.
>
> **② 인증 없이 토큰을 주는 엔드포인트가 없습니다.**
> `/api/auth/demo-login` 같은 걸 만들면 **그건 백도어**입니다. 만들지 않았습니다. `permitAll`은 `/health`, Swagger, `POST /api/auth/login` 뿐이고 나머지는 전부 `authenticated()`입니다.
> 프론트의 '회원가입 없이 둘러보기' 버튼도 `admin`/`admin`을 폼에 채워 **정규 로그인 API를 그대로 호출**합니다. 별도 경로가 아닙니다.
>
> **③ 이메일 형식 예외를 리터럴 1건으로 가뒀습니다.**
> `admin`은 이메일 형식이 아니라서 로그인 DTO의 `@Email`을 통과 못 합니다. 그렇다고 `@Email`을 그냥 빼면 **모든 비이메일 문자열이 통과**합니다. 그래서 커스텀 제약을 만들었습니다.
>
> ```java
> return DEMO_ACCOUNT_EMAIL.equals(value) || EMAIL_PATTERN.matcher(value).matches();
> ```
>
> **완전 일치(`equals`)** 입니다. 접두사 매칭이나 부분 일치가 아닙니다. 그리고 이 `@LoginEmail` 제약은 **로그인 DTO 딱 한 곳에만** 붙어 있습니다 — `grep`으로 확인했습니다. 멤버 생성 DTO는 표준 `@Email`이라 `admin` 같은 문자열은 **400으로 거부**됩니다."

**꼬리질문: "그래도 누구나 admin으로 들어와서 데이터를 지울 수 있잖아요."**

> "맞습니다. **그게 데모 계정의 본질적 성격**입니다. 이건 보안 결함이 아니라 **의도된 공개 데모**입니다.
>
> 다만 그렇기 때문에 지켜야 할 선이 있다고 봅니다.
> ① **실제 사용자 데이터가 없어야 합니다.** 이 DB에는 시드된 가상 데이터만 있습니다.
> ② **인증 메커니즘 자체는 정상이어야 합니다.** 비밀번호가 약할 뿐 검증은 제대로 돕니다. 즉 '이 사람이 인증을 우회하는 코드를 짜는 사람인가'라는 면접관의 질문에는 '아니오'라고 답할 수 있습니다.
> ③ **실서비스라면 절대 이렇게 안 합니다.** 데모 환경을 분리하고, 데모 계정에 읽기 전용 롤을 주거나, 주기적으로 데이터를 리셋할 겁니다.
>
> 참고로 저는 이전 프로젝트(`newGym`)에서 **인증 없이 ADMIN 계정을 생성하는 백도어 엔드포인트를 발견해서 제거**한 적이 있습니다. 그 경험 때문에 이번엔 처음부터 '데모 편의'와 '인증 우회'의 선을 명확히 긋고 시작했습니다."

**⚠️** 마지막 문단이 강력하다. **"나는 백도어를 만드는 사람이 아니라 찾아서 없애는 사람"** 이라는 서사가 된다.

---

### Q5-5. 로그인 실패 메시지를 왜 하나로 통일했나요?

**답변**

> "**사용자 열거(user enumeration) 공격**을 막기 위해서입니다.
>
> '이메일이 존재하지 않습니다'와 '비밀번호가 틀렸습니다'를 구분해서 주면, 공격자가 이메일 목록을 넣어보면서 **어떤 계정이 실제로 존재하는지 알아낼 수 있습니다.** 그 목록으로 크리덴셜 스터핑이나 피싱을 합니다.
>
> 그래서 세 경우 — **계정 없음 / 비밀번호 불일치 / 비활성 계정** — 을 전부 같은 `InvalidCredentialsException`으로 던지고 같은 401 메시지를 줍니다."

**꼬리질문 (심화, 나오면 가점): "메시지는 같은데 응답 시간은 어떤가요?"**

> "**날카로운 지적입니다. 그게 이 코드에 남아 있는 한계입니다.**
>
> 계정이 없으면 `orElseThrow`로 **bcrypt 비교 없이 즉시** 예외를 던지고, 계정이 있으면 bcrypt를 돕니다. BCrypt는 의도적으로 느린 알고리즘이라 수십~수백 밀리초가 걸립니다. 즉 **응답 시간 차이로 계정 존재 여부가 구분됩니다.**
>
> 배포 전 검수에서 이걸 🟡 항목으로 스스로 기록해뒀습니다. 표준 대응은 **계정이 없을 때도 더미 해시로 `matches()`를 한 번 돌린 뒤 실패시키는 것**입니다.
>
> 여기서 배운 게, **사이드 채널은 응답 내용뿐 아니라 응답 시간과 크기도 정보**라는 겁니다. 메시지 통일까지만 하고 '보안 처리 끝'이라고 생각했던 게 안일했습니다."

**⚠️** 이 답변은 **면접관이 안 물어봐도 먼저 말하면 좋다.** "스스로 한계를 찾는 사람"이라는 신호가 가장 강하게 전달되는 지점이다.

---

### Q5-6. CORS는 어떻게 설정하셨나요?

**답변**

> "**명시적 화이트리스트만** 허용했습니다.
>
> ```java
> configuration.setAllowedOrigins(allowedOrigins);   // 환경변수 CORS_ALLOWED_ORIGINS
> configuration.setAllowedMethods(List.of("GET","POST","PATCH","PUT","DELETE","OPTIONS"));
> configuration.setAllowedHeaders(List.of("Authorization","Content-Type"));
> ```
>
> **와일드카드 `*`는 쓰지 않았습니다.** `*`는 '인터넷의 모든 사이트가 이 API를 호출해도 된다'는 뜻입니다. 메서드와 헤더도 필요한 것만 목록으로 제한했습니다.
>
> `allowCredentials`는 켜지 않았습니다(기본 false). 쿠키를 안 쓰니 필요 없고, 그래서 `*`와 `credentials: true`의 위험한 조합 문제도 원천적으로 없습니다.
>
> 오리진 목록은 **환경변수(`CORS_ALLOWED_ORIGINS`)** 로 주입합니다. 로컬은 `http://localhost:5173`, 배포는 Vercel 도메인입니다. 코드를 안 고치고 환경별로 바뀝니다."

**꼬리질문: "CORS가 보안 기능인가요?"**

> "**엄밀히는 브라우저의 보호 장치이지 서버의 방어선이 아닙니다.**
>
> CORS는 '브라우저에서 실행되는 스크립트가 다른 오리진의 응답을 읽는 것'을 제한합니다. curl이나 서버 간 호출은 CORS와 무관하게 그냥 됩니다.
>
> 그래서 **CORS를 인증 대신 쓰면 안 됩니다.** 이 프로젝트의 실제 방어선은 JWT 검증과 `@PreAuthorize`이고, CORS는 '브라우저에서 다른 사이트가 우리 API를 호출해 응답을 읽는 시나리오'를 좁히는 보조 수단입니다."

---

### Q5-7. 에러 응답 설계는 어떻게 하셨나요?

**답변**

> "**RFC 9457 `ProblemDetail`** 로 전부 통일했습니다. Spring 6부터 내장돼 있어서 커스텀 에러 DTO를 따로 만들지 않았습니다.
>
> ```json
> {
>   "type": "about:blank",
>   "title": "입력값이 올바르지 않습니다",
>   "status": 400,
>   "detail": "요청 본문 검증에 실패했습니다",
>   "instance": "/api/members",
>   "errors": [{ "field": "email", "message": "올바른 이메일 형식이 아닙니다" }]
> }
> ```
>
> `@RestControllerAdvice` 하나가 전부 변환합니다. 400(검증) / 401(인증) / 403(권한) / 404 / 409(중복) / 500.
>
> **500에서 특히 신경 쓴 게 있습니다.**
>
> ```java
> String traceId = UUID.randomUUID().toString();
> log.error("처리되지 않은 예외 [traceId={}]", traceId, ex);   // 스택트레이스는 서버 로그에만
> problem.setProperty("traceId", traceId);                      // 클라이언트에는 추적 ID만
> ```
>
> **스택트레이스나 예외 메시지를 절대 응답에 싣지 않습니다.** 내부 클래스 구조, 라이브러리 버전, SQL 문장이 새어나가면 공격자에게 시스템 지도를 그려주는 셈입니다. 대신 `traceId`를 주면 사용자가 문의할 때 그 ID로 서버 로그에서 정확히 찾을 수 있습니다."

---

## 6. 트러블슈팅 스토리 (STAR)

> **STAR = Situation(상황) → Task(과제) → Action(행동) → Result(결과)**
> 트러블슈팅 질문에는 이 구조로 답한다. 아래 5개는 통째로 외워둘 만하다.

### Q6-1. ★ 가장 기억에 남는 트러블슈팅은?

**추천: Render 512MB JVM 메모리 튜닝** (인프라 이해도가 드러나서 가장 강하다)

> **[상황]** 제 워크스페이스 규칙상 Spring 프로젝트는 Render 무료 티어(512MB)에 안 올리고 로컬 쇼케이스로 두게 되어 있었습니다. JVM이 무거워서 OOM 위험이 있다는 이유였습니다. 그런데 포트폴리오에서 백엔드가 라이브로 안 뜨면 면접관이 실제 동작을 못 보니, **실측해보고 안 되면 그때 포기하자**고 판단했습니다.
>
> **[과제]** Spring Boot 앱을 512MB 컨테이너 안에서 안정적으로 기동시키는 것.
>
> **[행동]**
> 처음엔 `-Xmx256m -XX:MaxMetaspaceSize=96m`으로 잡았습니다. 힙을 절반으로 제한하면 되겠거니 한 거죠. 그런데 **기동 도중에 `java.lang.OutOfMemoryError: Metaspace`로 죽었습니다.**
>
> 여기서 제가 잘못 알고 있던 걸 발견했습니다. **`-Xmx`는 힙만 제한합니다.** 메타스페이스는 **힙 밖의 네이티브 메모리**에 있고, 여기에는 **로드된 클래스의 메타데이터**가 들어갑니다.
>
> 그리고 Spring Boot는 클래스를 엄청나게 많이 로드합니다. 컴포넌트 스캔으로 클래스패스를 전수 조사하고, 자동 설정을 평가하고, `@Transactional`·`@PreAuthorize`용 **프록시 클래스를 동적으로 생성**합니다. 여기에 Hibernate가 엔티티 메타모델을 만들고, springdoc이 컨트롤러를 전부 스캔합니다. 96MB로는 부족했던 겁니다.
>
> 그래서 **메타스페이스를 180MB로 올리고, GC를 SerialGC로 바꿨습니다.** G1GC(기본값)는 병렬 처리를 위한 자료구조와 GC 스레드가 메모리를 더 먹는데, 무료 티어는 CPU도 공유 1코어 수준이라 병렬화 이득이 거의 없습니다. **작은 힙에서는 SerialGC가 오버헤드가 적습니다.**
>
> **[결과]**
> 기동에 성공했고, API를 반복 호출한 뒤 **RSS ~396MB에서 안정**되는 걸 확인했습니다. 512MB 안에 들어옵니다. 배분은 힙 256 + 메타 180 = 436MB, 나머지 ~76MB를 스레드 스택·JIT 코드 캐시·네이티브 버퍼 여유로 남겼습니다.
>
> **배운 것**은 두 가지입니다. **컨테이너 메모리 = 힙 + 메타스페이스 + 스레드 스택 + JIT 코드 캐시 + 네이티브 버퍼**라는 것, 그리고 **'JVM은 무겁다'는 통념도 수치로 검증하면 다룰 수 있는 문제가 된다**는 것입니다. 규칙을 무시한 게 아니라 규칙의 전제를 실측으로 확인한 겁니다.

**꼬리질문: "메모리를 더 줄일 방법은 없나요?"**

> "몇 가지 있습니다.
> ① **springdoc을 운영 프로파일에서 빼면** 클래스 로딩이 눈에 띄게 줄어듭니다. 다만 포트폴리오라 Swagger가 보여야 해서 유지했습니다.
> ② **CDS(Class Data Sharing)** 로 클래스 메타데이터를 공유 아카이브로 만들면 메타스페이스와 기동 시간을 줄일 수 있습니다.
> ③ **GraalVM 네이티브 이미지**가 가장 극적입니다. 기동이 수십 ms, 메모리가 수십 MB 수준으로 떨어집니다. 대신 리플렉션 힌트를 명시해야 하고 빌드가 오래 걸립니다. Spring Boot 3부터 AOT 지원이 있어서 다음에 시도해보고 싶은 항목입니다."

---

### Q6-2. Jackson 3 전환으로 앱이 안 뜬 이야기

> **[상황]** Spring Boot 4로 프로젝트를 시작했는데, `SecurityConfig`에서 `ObjectMapper`를 주입받는 코드를 쓰자 애플리케이션이 기동되지 않았습니다.
>
> **[과제]** 빈 생성 실패 원인 파악.
>
> **[행동]** 에러를 보니 주입 대상 빈을 못 찾는다는 내용이었는데, `ObjectMapper`는 Spring Boot가 자동으로 등록해주는 빈이라 이상했습니다. 확인해보니 **Spring Boot 4는 Jackson 3을 씁니다.** Jackson 3은 패키지 루트가 `com.fasterxml.jackson`에서 **`tools.jackson`** 으로 바뀌었습니다.
>
> 제가 습관대로 `com.fasterxml.jackson.databind.ObjectMapper`를 import했는데, 스프링이 등록한 빈은 `tools.jackson.databind.ObjectMapper` 타입이라 **타입이 달라서 주입 대상이 없었던** 겁니다. 두 클래스는 이름만 같고 완전히 다른 타입입니다.
>
> **[결과]** import를 `tools.jackson.databind.ObjectMapper`로 바꿔서 해결했습니다.
>
> **여기서 얻은 게 하나 더 있습니다.** 이 경험 때문에 JWT 라이브러리 선택이 바뀌었습니다. 원래 jjwt를 쓰려 했는데, **`jjwt-jackson`이 Jackson 2 바인딩**이라 같은 충돌을 일으킬 게 뻔했습니다. Jackson 2를 클래스패스에 되살리거나 `jjwt-gson`으로 우회해야 하는데, 어느 쪽이든 JSON 바인딩이 두 개가 됩니다. 그래서 **Spring 내장 Nimbus**로 방향을 틀었고, 결과적으로 의존성도 줄고 커스텀 필터도 안 만들게 됐습니다.
>
> **배운 것**: 메이저 버전 업그레이드에서 **패키지 루트가 바뀌는 변경**은 IDE 자동완성이 오히려 함정이 됩니다(옛 패키지도 전이 의존성으로 클래스패스에 남아 있을 수 있습니다). 그리고 **한 라이브러리의 변경이 다른 라이브러리 선택까지 연쇄적으로 바꾼다**는 걸 경험했습니다.

---

### Q6-3. `Failed to select a JWK signing key` — HS256 헤더 명시

> **[상황]** JWT 발급 코드를 다 짰는데 로그인 API를 호출하면 `Failed to select a JWK signing key` 예외가 났습니다.
>
> **[과제]** HS256 대칭키로 토큰을 발급하는 것.
>
> **[행동]** 원인이 **두 겹**이었습니다.
>
> ① `OctetSequenceKey`(대칭키 JWK)를 만들 때 **알고리즘을 지정하지 않으면** 인코더가 여러 키 중 어느 걸 쓸지 못 고릅니다.
> ② 더 헷갈렸던 건 `JwtEncoderParameters.from(claims)`만 쓰면 **JWS 헤더의 알고리즘 기본값이 RS256**으로 잡힌다는 거였습니다. HS256 대칭키 JWK와 매칭이 안 됩니다.
>
> 그래서 **양쪽 모두** 명시했습니다.
>
> ```java
> // JwtConfig — JWK에 alg 명시
> JWK jwk = new OctetSequenceKey.Builder(jwtSecretKey)
>     .algorithm(JWSAlgorithm.HS256)
>     .keyID(UUID.randomUUID().toString())
>     .build();
>
> // TokenService — JWS 헤더에도 명시
> JwsHeader header = JwsHeader.with(MacAlgorithm.HS256).build();
> jwtEncoder.encode(JwtEncoderParameters.from(header, claims));
> ```
>
> **[결과]** 정상 발급됐습니다. 두 곳 모두에 **왜 명시해야 하는지를 주석으로** 남겼습니다.
>
> **배운 것**: Spring Security의 JWT 지원은 **RSA 비대칭 시나리오를 1급으로 가정**하고 설계돼 있습니다. 편의 API의 기본값이 항상 내 시나리오와 맞지는 않는다는 걸 배웠습니다. 그리고 에러 메시지("키를 못 고르겠다")가 실제 원인("헤더 알고리즘이 다르다")과 다른 층위를 가리킬 수 있다는 것도요.

---

### Q6-4. ★ 검수에서 발견한 버그 — 운영자 권한이 조용히 강등되던 React 버그

> **이 스토리는 프론트 이해도 + 디버깅 능력 + 코드 리뷰 태도를 한 번에 보여준다.**

> **[상황]** 배포 전 검수에서 blocker 3건이 나왔는데, 그중 하나가 **멤버 수정 모달**이었습니다. 증상이 두 가지였습니다.
>
> - 아무것도 안 고치고 저장 → **400 에러**
> - 운영자의 **이름만** 바꾸고 저장 → **역할이 멤버로 강등됨**
>
> **[과제]** 원인 파악과 수정. 특히 두 번째는 **데이터 손상**이라 심각도가 높았습니다.
>
> **[행동]** 원인이 React 실수 **두 개가 겹친** 거였습니다.
>
> **첫째, `key`를 잘못된 곳에 걸었습니다.**
> `EditMemberModal`이 조건 없이 **항상 렌더링**되고 있었고, `key`는 컴포넌트 자체가 아니라 **내부의 `<Modal>`** 에 붙어 있었습니다. 그래서 `EditMemberModal`은 **최초 1회만 마운트**되고, 그 시점의 `member` prop은 `null`이었습니다.
>
> ```tsx
> const [name, setName] = useState(member?.name ?? ""); // → "" 로 고정
> const [role, setRole] = useState(member?.role ?? "MEMBER"); // → "MEMBER" 로 고정
> ```
>
> **`useState`의 인자는 '초기값'이지 '현재값'이 아닙니다.** 마운트 시점에 한 번만 평가되고, 이후 prop이 바뀌어도 state는 절대 자동으로 따라가지 않습니다.
>
> **둘째, 제어 컴포넌트와 비제어 컴포넌트를 섞었습니다.**
> 입력창이 `defaultValue={member?.name}`(비제어)였습니다. `defaultValue`도 초기값일 뿐이지만, 렌더링 타이밍상 **화면에는 올바른 값이 보였습니다.** 그래서 **화면은 멀쩡한데 state는 계속 초기값**이었고, 제출 시 `{name: "", role: "MEMBER"}`가 그대로 전송된 겁니다.
>
> - `name: ""` → 서버 `@Size(min=1)` 위반 → 400
> - `role: "MEMBER"` → **ORGANIZER 강등**
>
> 재미있는 건, 데모 시드에 운영자가 admin 하나뿐이라 서버의 '**마지막 운영자 보호**' 가드에 걸려서 409 "운영자가 최소 1명 필요합니다"라는 **이름 수정과 전혀 무관한 에러**가 떴다는 겁니다. 이 엉뚱한 에러 메시지가 오히려 힌트가 됐습니다 — "이름만 고쳤는데 왜 운영자 개수 얘기가 나오지?"
>
> **수정은 네 가지**를 했습니다.
> ① `{editTarget && <EditMemberModal key={editTarget.id} ... />}` — **조건부 렌더 + key를 컴포넌트 자체에** 걸어서 대상마다 재마운트
> ② prop 타입을 non-nullable로 바꿔 `useState(member.name)`이 항상 유효한 값을 받게
> ③ 입력을 전부 **제어 컴포넌트(`value` + `onChange`)** 로 통일
> ④ PATCH 의미에 맞게 **실제로 바뀐 필드만 전송**, 변경이 없으면 요청 자체를 안 보냄
>
> **[결과]** 두 증상 모두 해결됐습니다.
>
> **배운 것**이 세 가지입니다.
> ① **`key`는 리스트 최적화 힌트가 아니라 컴포넌트의 정체성**입니다. key가 바뀌면 React가 인스턴스를 버리고 새로 마운트하므로 `useState` 초기값이 다시 평가됩니다. '편집 대상이 바뀌면 폼이 새로 시작된다'는 요구사항에는 이 재마운트가 정확한 도구였습니다.
> ② **제어/비제어를 섞으면 화면과 state가 갈라집니다.** 그리고 그게 이 버그의 진짜 무서운 점이었습니다 — **화면상으로는 아무 문제가 없어 보이는데 권한이 조용히 손상**됩니다.
> ③ **테스트로 안 잡히는 종류의 버그**입니다. 프론트 테스트가 없었고, 있었어도 "저장 버튼을 누르면 API가 호출된다"까지만 검증했다면 통과했을 겁니다. 실제로 시나리오를 끝까지 따라가는 검수가 필요했습니다.

**꼬리질문: "`useEffect`로 prop 변경 시 state를 동기화하면 안 되나요?"**

> "가능하지만 두 가지 문제가 있습니다. 렌더가 한 번 일어난 **뒤에** 동기화되므로 중간에 낡은 값이 보이는 순간이 생기고, 의존성 배열 관리가 늘어납니다.
>
> React 공식 문서도 이런 경우 **'key로 상태를 리셋하는 것'** 을 우선 권장합니다. 여기서는 '편집 대상이 바뀌면 폼 전체가 처음부터 시작된다'가 정확한 의미라서 key가 더 맞는 표현이었습니다."

---

### Q6-5. 검수에서 발견한 계약 불일치 — 조회는 되는데 저장은 안 되는 회차

> **[상황]** 검수에서 나온 두 번째 blocker입니다. 멤버를 비활성화하면, **그 멤버의 기록이 있는 과거 회차는 출석을 다시 저장할 수 없게** 됐습니다. 400 에러가 나고 그 회차는 영구히 저장 불가 상태가 됩니다.
>
> **[과제]** 원인 파악과 수정.
>
> **[행동]** 원인은 **두 API가 서로 다른 규칙을 갖고 있던 것**이었습니다.
>
> | 엔드포인트                                  | 비활성 멤버 규칙                                                      |
> | ------------------------------------------- | --------------------------------------------------------------------- |
> | `GET /api/sessions/{id}` (회차 상세)        | **기록이 있는 비활성 멤버는 포함** (과거 기록을 화면에서 안 지우려고) |
> | `PUT /api/sessions/{id}/attendances` (저장) | **활성 멤버만 허용**                                                  |
>
> 프론트는 저장할 때 **화면에 뜬 전원**을 그대로 보냅니다. 즉 **"화면에 보이는데 저장은 안 되는"** 상태가 된 겁니다.
>
> 각 API만 따로 보면 **둘 다 타당해 보입니다.** 조회는 과거 기록을 보존해야 하고, 저장은 비활성 멤버를 새로 추가하면 안 되니까요. **엔드포인트 단위로 리뷰하면 절대 안 잡히고, 한 사용자 시나리오를 끝까지 따라가야** 발견되는 종류였습니다.
>
> **수정은 서버 쪽을 택했습니다.**
>
> ```java
> boolean allExistAndAllowed = requestedIds.stream()
>     .allMatch(id -> membersById.containsKey(id)
>         && (membersById.get(id).isActive() || existingByMember.containsKey(id)));
> ```
>
> "**활성 멤버 또는 이 회차에 이미 기록이 있는 멤버**"로 조건을 정확히 했습니다.
>
> **왜 프론트가 아니라 서버였냐면** 세 가지 이유입니다.
> ① 어차피 `existingByMember` 맵을 이미 1회 조회해둬서 **추가 쿼리 0건**으로 판정할 수 있었습니다.
> ② 프론트에서 payload를 거르면 화면 하나는 고쳐지지만 **서버 계약의 모순은 그대로** 남습니다. 다른 클라이언트가 붙으면 재발합니다.
> ③ 보안적으로도 서버가 최종 판정자여야 합니다.
>
> **중요한 건 규칙을 느슨하게 푼 게 아니라는 겁니다.** '이 회차에 기록이 **없는** 비활성 멤버를 새로 체크'하는 건 여전히 400으로 막힙니다. 두 케이스를 구분하는 **회귀 테스트 2개**를 함께 추가했습니다.
>
> 그리고 **API 문서 두 곳(#12·#14)에 '비활성 멤버 취급 규칙'을 한 문장으로 명시**했습니다.
>
> **[결과]** 저장이 정상 동작하고, 테스트로 잠겼고, 문서가 서로 다른 말을 하지 않게 됐습니다.
>
> **배운 것**: 이건 **코드 버그이기 전에 명세 버그**였습니다. 같은 도메인 개념(비활성 멤버)에 대해 두 API가 다른 규칙을 갖고 있었고, 각각은 합리적이었습니다. **고칠 때 코드만 고치고 문서를 안 고쳤다면 6개월 뒤에 똑같은 버그가 재발**했을 겁니다.

---

## 7. 프론트엔드

### Q7-1. Zustand와 TanStack Query를 어떻게 나눠 쓰셨나요?

**답변**

> "**상태의 소유자가 누구냐**로 나눴습니다.
>
> |      | 클라이언트 상태                        | 서버 상태                                  |
> | ---- | -------------------------------------- | ------------------------------------------ |
> | 정의 | 이 브라우저에만 존재, 내가 유일한 주인 | **서버가 주인**, 브라우저에 있는 건 복사본 |
> | 예   | 토큰, 로그인 멤버, 모달 열림 여부      | 멤버 목록, 회차 상세, 누적 벌금            |
> | 도구 | **Zustand**                            | **TanStack Query**                         |
>
> Zustand에는 **토큰과 현재 로그인 멤버만** 둡니다. `persist` 미들웨어로 localStorage에 동기화해서 새로고침해도 로그인이 유지됩니다.
>
> **서버 데이터는 전부 TanStack Query가 소유**합니다. 캐싱, 로딩/에러 상태, 그리고 **무효화**를 다 해주기 때문입니다. 출석을 저장하면 관련 쿼리를 무효화해서 멤버 목록의 누적 벌금과 회차 목록의 합계가 저절로 최신화됩니다.
>
> **서버 데이터를 Zustand에 복사하면 안 되는 이유**가 여기 있습니다. 그러면 캐시가 두 개가 되고, 무효화가 Zustand까지 도달하지 못해서 **화면 두 곳이 다른 숫자를 보여주게** 됩니다."

---

### Q7-2. Zod는 왜 쓰셨나요? TypeScript 타입이 있는데요.

**답변**

> "**TypeScript 타입은 컴파일 타임에만 존재**하기 때문입니다. 빌드하고 나면 사라집니다.
>
> 서버가 예상과 다른 모양의 JSON을 주면 TS는 아무것도 못 막습니다. `response.data.accumulatedFine`이 `undefined`인 채로 흘러가서, **한참 뒤 엉뚱한 컴포넌트에서 렌더 에러**로 터집니다. 원인 추적이 어렵죠.
>
> Zod로 응답을 `parse`하면 **파싱 지점에서 명확한 에러**가 납니다. 백엔드 스펙 변경을 조기에 잡는 겁니다.
>
> 그리고 `z.infer`로 **스키마에서 타입을 뽑아내기 때문에** 스키마와 타입이 절대 어긋나지 않습니다. 타입을 따로 선언하면 스키마만 고치고 타입을 안 고치는 실수가 생기는데, 그게 구조적으로 불가능합니다.
>
> 폼 검증과 응답 파싱에 **같은 도구**를 쓰는 것도 이점입니다. 실제로 로그인 폼의 `admin` 예외 처리도 서버의 `@LoginEmail`과 대칭으로 Zod 스키마에 넣었습니다."

---

### Q7-3. axios 인터셉터에서 로그인 요청의 401만 제외한 이유는?

**답변**

> "**의미가 다른 401이기 때문**입니다.
>
> 응답 인터셉터는 401을 받으면 '토큰이 만료됐거나 위조됐다'고 보고 로그아웃 + 로그인 페이지로 보냅니다. 그런데 **로그인 시도에서 비밀번호가 틀려도 401**이 옵니다.
>
> 이때 리다이렉트를 하면 사용자가 방금 입력한 폼이 날아가고 '왜 튕기지?' 하게 됩니다. 이 401은 **로그인 폼이 에러 메시지로 처리해야 할** 것이지 세션 만료가 아닙니다.
>
> ```ts
> const isLoginRequest = error.config?.url?.includes("/api/auth/login");
> if (!isLoginRequest) {
>   logout();
>   redirect();
> }
> ```
>
> 이미 로그아웃 상태에서 로그인을 시도하는 중이니 토큰도 없고, 인터셉터가 할 일이 애초에 없습니다."

---

### Q7-4. 출석 체크 화면에서 벌금 미리보기는 어떻게 하셨나요? 계산을 프론트에서도 하나요?

**답변**

> "**미리보기는 프론트, 확정은 서버**로 명확히 나눴습니다.
>
> 사용자가 '지각'을 누르면 즉시 금액이 보여야 UX가 좋습니다. 그래서 `GET /api/study-room`으로 받은 현재 단가를 써서 **표시만** 합니다.
>
> 그런데 **저장 요청에는 `fineAmount`를 아예 보내지 않습니다.**
>
> ```json
> { "attendances": [{ "memberId": 1, "status": "LATE" }] } // 금액 필드 없음
> ```
>
> 클라이언트가 금액을 보낼 수 있으면 **조작 가능**해집니다. 사용자가 개발자 도구로 `fineAmount: 0`을 보내면 벌금이 사라지죠.
>
> 저장 후에는 **서버 응답의 `fineAmount`로 화면을 교체**합니다. **벌금 계산의 단일 출처는 서버의 `FinePolicy`** 이고, 프론트의 계산은 어디까지나 낙관적 미리보기입니다.
>
> 계산 규칙이 두 곳에 존재하면 **반드시 어긋납니다.** 단가가 바뀌었는데 프론트가 캐시된 옛 단가를 쓰고 있으면 미리보기와 실제 금액이 달라지죠. 그래서 저장 응답으로 덮어쓰는 게 중요합니다."

---

### Q7-5. 프론트에서 역할별 라우팅을 했는데, 그럼 보안이 이중으로 된 건가요?

**답변**

> "**아닙니다. 프론트 가드는 보안이 아니라 UX입니다.** 이건 명확히 하고 싶습니다.
>
> `RequireOrganizer`는 MEMBER로 로그인했을 때 운영자 메뉴를 안 보여주는 역할만 합니다. 하지만 브라우저 자바스크립트는 사용자가 얼마든지 조작할 수 있습니다. 컴포넌트를 우회하거나, 아예 콘솔에서 `fetch`로 API를 직접 호출하면 그만입니다.
>
> **실제 방어선은 서버의 `@PreAuthorize` 하나뿐**입니다. 그래서 서버 권한 매트릭스를 15개 엔드포인트 전부에 대해 문서화하고, 그중 핵심을 테스트로 잠갔습니다.
>
> 코드 주석에도 '이건 UX 편의일 뿐 실제 보안 경계는 서버'라고 적어뒀습니다. **프론트 가드를 보안으로 착각하는 게 흔한 실수**라고 알고 있어서, 착각하지 않았다는 걸 코드에 남기고 싶었습니다."

---

## 8. 배포 / 인프라 / JVM

### Q8-1. Docker 멀티스테이지 빌드를 왜 쓰셨나요?

**답변**

> "세 가지 이득이 있습니다.
>
> ① **이미지 크기.** 빌드 단계는 JDK가 필요하지만 실행에는 JRE만 있으면 됩니다. 최종 이미지에 JDK·Maven·소스코드가 안 들어가서 수백 MB가 줄어듭니다.
> ② **보안 표면.** 운영 컨테이너에 컴파일러와 빌드 도구가 없습니다.
> ③ **레이어 캐싱.** 이게 실용적으로 제일 큽니다.
>
> ```dockerfile
> COPY .mvn .mvn
> COPY mvnw pom.xml ./
> RUN ./mvnw -q dependency:go-offline    # ← 의존성만 받는 레이어
> COPY src ./src                          # ← 소스는 그 다음
> RUN ./mvnw -q -DskipTests package
> ```
>
> `pom.xml`만 먼저 복사해서 의존성을 받는 순서가 핵심입니다. 소스 코드만 바뀌면 **의존성 다운로드 레이어는 캐시가 재사용**되어 빌드가 훨씬 빠릅니다. npm 프로젝트에서 `package.json`만 먼저 COPY하고 `npm ci` 하는 것과 같은 패턴입니다."

---

### Q8-2. ★ JVM 메모리 구조를 설명해주세요.

**답변** → **[Q6-1](#q6-1--가장-기억에-남는-트러블슈팅은)의 스토리와 함께 답한다.**

> "크게 **힙과 힙 밖**으로 나뉩니다.
>
> ```
> 컨테이너 메모리
> ├── 힙(Heap)              -Xmx로 제한. new 로 만든 객체. GC 대상
> ├── 메타스페이스(Metaspace) -XX:MaxMetaspaceSize. 로드된 "클래스 자체"의 메타데이터. 힙 밖!
> ├── 스레드 스택            스레드당 ~1MB × N (톰캣 워커, GC 스레드)
> ├── 코드 캐시(JIT)         JIT가 컴파일한 네이티브 코드
> ├── GC 자체 오버헤드       알고리즘마다 다름
> └── 네이티브 버퍼          NIO 다이렉트 버퍼, JDBC 드라이버 등
> ```
>
> **가장 중요한 건 `-Xmx`가 힙만 제한한다는 것**입니다. 나머지는 전부 힙 밖입니다. 그래서 '힙을 줄였는데 왜 컨테이너가 죽지?'가 발생합니다."

**꼬리질문: "왜 JVM이 Node보다 무거운가요?"**

> "**클래스 로딩 방식이 근본적으로 다릅니다.**
>
> Node는 JS 소스를 파싱해서 V8 힙에 올립니다. JVM은 **클래스 파일을 메타스페이스에 로드**하고, 각 클래스의 구조 정보를 메모리에 유지합니다.
>
> 여기에 Spring Boot가 얹히면:
>
> - **컴포넌트 스캔** — 클래스패스를 전수 조사
> - **자동 설정 평가** — 수백 개의 조건부 설정 클래스를 로드
> - **프록시 동적 생성** — `@Transactional`, `@PreAuthorize`가 붙은 빈마다 프록시 클래스를 런타임에 만듭니다
> - **Hibernate 엔티티 메타모델**
> - **springdoc의 컨트롤러 전수 스캔**
>
> 전부 클래스 로딩이고, 전부 메타스페이스를 씁니다. 그래서 Node 백엔드가 30~~80MB로 뜨는 반면 Spring Boot는 250~~400MB가 기본입니다.
>
> 대신 얻는 게 있습니다. **JIT 컴파일러가 실행 중에 최적화**해서 워밍업 후 처리량은 높고, 타입 안전성과 성숙한 생태계가 있습니다. **부팅 비용과 런타임 성능의 트레이드오프**라고 봅니다."

---

### Q8-3. GC를 왜 SerialGC로 바꾸셨나요?

**답변**

> "**작은 힙 + 저사양 CPU 환경에서는 SerialGC가 유리**하기 때문입니다.
>
> | GC                  | 특징                          | 이 환경에서                                                                |
> | ------------------- | ----------------------------- | -------------------------------------------------------------------------- |
> | G1GC (Java 9+ 기본) | 병렬, 리전 기반, 큰 힙에 최적 | 자체 자료구조(리전 테이블, 리멤버드 셋)와 GC 스레드가 **메모리를 더 먹음** |
> | SerialGC            | 단일 스레드, 자료구조 최소    | ✅ 오버헤드 최소                                                           |
>
> Render 무료 티어는 CPU도 공유 1코어 수준이라 **GC 병렬화의 이득이 거의 없습니다.** 이득은 없는데 GC 자체의 메모리 오버헤드는 그대로 나가는 상황이라, 힙이 256MB밖에 안 되는 환경에서는 SerialGC가 합리적이었습니다.
>
> 물론 힙이 크고 코어가 많은 운영 환경이라면 G1GC나 ZGC가 맞습니다. **환경에 따라 답이 다른 문제**입니다."

---

### Q8-4. Render 콜드스타트는 어떻게 처리하셨나요?

**답변**

> "**해결하지 않고 명시했습니다.** 무료 티어의 구조적 제약이라 코드로 못 없앱니다.
>
> Render 무료 티어는 **15분 미접속 시 슬립**하고, 다음 요청에서 깨어나는 데 30~60초 걸립니다. JVM 부팅까지 겹치면 첫 로그인이 느립니다.
>
> 그래서 README에 **'첫 로그인이 느리면 콜드스타트 때문이니 새로고침하지 말고 잠깐 기다리면 된다'** 고 적었습니다. 숨기는 것보다 낫다고 봤습니다.
>
> 개선한다면 세 가지 선택지가 있습니다.
> ① **유료 티어**로 상시 가동 — 가장 단순
> ② **외부 cron으로 주기적 핑** — 다만 계정 전체가 월 750 무료 인스턴스 시간을 공유하므로, 이 프로젝트만 살려두면 다른 포트폴리오 백엔드의 가동 시간을 잡아먹습니다. **트레이드오프가 있는 해법**입니다.
> ③ **GraalVM 네이티브 이미지** — 기동이 수십 ms 수준으로 떨어져서 콜드스타트 자체가 문제가 안 됩니다. 가장 근본적이지만 리플렉션 힌트 설정과 긴 빌드 시간이 대가입니다."

---

### Q8-5. 환경변수와 시크릿은 어떻게 관리하셨나요?

**답변**

> "**전부 환경변수로 분리하고 `.env`는 커밋하지 않았습니다.**
>
> `application.yml`은 전부 `${...}` 참조만 하고 실제 값이 하나도 없습니다.
>
> ```yaml
> app:
>   jwt:
>     secret: ${JWT_SECRET}
>     expiration-hours: ${JWT_EXPIRATION_HOURS:12}
>   cors:
>     allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:5173}
> ```
>
> 저장소에는 **`.env.example`만** 두고 키 이름과 설명 주석을 넣었습니다. 검수에서 `git log --all --full-history -- '**/.env'`로 **커밋 이력에 `.env`가 한 번도 없었던 것까지** 확인했습니다.
>
> 그리고 하나 더 신경 쓴 게 있습니다. **`JWT_SECRET`이 32바이트 미만이면 애플리케이션 기동 자체를 실패**시킵니다.
>
> ```java
> @PostConstruct
> void validateSecretLength() {
>     if (secret == null || secret.getBytes(UTF_8).length < 32)
>         throw new IllegalStateException("JWT_SECRET은 HS256 최소 요구치인 32바이트 이상이어야 합니다...");
> }
> ```
>
> HS256의 최소 키 길이 요구를 **로그인 요청이 들어온 순간에 발견하는 게 아니라 부팅 시점에** 잡으려고요. '늦은 실패보다 빠른 실패'입니다."

---

### Q8-6. CI는 어떻게 구성하셨나요?

**답변**

> "GitHub Actions로 **잡 2개를 병렬**로 돌립니다.
>
> - `server`: Temurin 21 셋업 → Maven 캐시 → `./mvnw -B -DskipTests package` → `./mvnw -B test`
> - `client`: Node 22 → `npm ci` → lint → `npm run build`(내부에 `tsc` 포함)
>
> **DB가 필요한 테스트가 없어서 Postgres 서비스 컨테이너를 안 띄웠습니다.** 필요 없는 걸 넣으면 CI가 느려지기만 합니다.
>
> 의존성 캐싱(`cache: maven`, `cache: npm`)은 넣었습니다. 매번 전체를 받으면 CI 시간이 몇 배가 됩니다.
>
> 한 가지 배운 게 있는데, **CI 그린이 로컬 재현 가능을 보장하지 않는다**는 겁니다. CI는 Temurin 21을 명시적으로 셋업하니 잘 돌았는데, 제 로컬에서는 `java`가 PATH에 없어서 `./mvnw test`가 즉시 실패했습니다. 그래서 README에 **JDK 21 전제와 `JAVA_HOME` 지정 방법**을 명시했습니다."

---

## 9. 테스트 / 품질

### Q9-1. 테스트를 어떻게 짜셨나요?

**답변**

> "**28개**이고 세 종류입니다.
>
> **① 순수 단위 테스트 — `FinePolicyTest`**
> 벌금 계산 로직입니다. `FinePolicy`가 스프링 빈이 아니고 DB도 모르는 static 함수라, **스프링 컨텍스트도 목도 없이** 테스트됩니다. 실행이 밀리초라 케이스를 많이 넣을 수 있었습니다.
> 특히 `snapshot_pastResultDoesNotChangeWhenRateChangesLater`가 **이 프로젝트의 제1 설계 원칙(벌금 스냅샷)을 테스트로 고정**합니다.
>
> **② 슬라이스 테스트 — 권한 가드**
> `@WebMvcTest` + `spring-security-test`의 `jwt()` 후처리기로, 각 엔드포인트에 대해 **토큰 없음 → 401 / MEMBER → 403 / ORGANIZER → 200**을 검증합니다.
> **API.md의 권한 매트릭스가 곧 이 테스트들의 명세**입니다. 문서와 테스트가 1:1로 대응합니다.
>
> **③ 목 기반 서비스 테스트 — `AttendanceServiceTest`**
> 검수에서 발견한 계약 불일치 버그를 고치면서 **회귀 방지용으로** 추가했습니다. 비활성 멤버가 허용되는 경우와 거부되는 경우를 정확히 구분합니다."

**꼬리질문: "테스트가 28개면 커버리지가 낮지 않나요?"**

> "**낮습니다. 그리고 의도적입니다.** S티어(하루) 규정상 통합 테스트는 면제고, 단위 테스트도 '핵심만'이 기준이었습니다.
>
> 다만 **어디를 덮었는지가 중요**하다고 봅니다. 이 프로젝트에서 틀리면 가장 아픈 두 가지가 **① 벌금 계산 규칙**과 **② 권한 가드**입니다. 벌금이 틀리면 도메인 가치가 사라지고, 권한이 뚫리면 보안 사고입니다. 이 둘을 우선 덮었습니다.
>
> 그리고 **덮지 못한 곳도 정확히 알고 있습니다.** 회차 API의 권한 가드 테스트가 통째로 없고, `AuthService` 자체도 목으로 대체돼서 테스트되지 않습니다. 검수 문서에 🟡 항목으로 기록해뒀습니다."

---

### Q9-2. `@WebMvcTest`에서 `@Import(SecurityConfig.class)`를 왜 하셨나요?

**답변**

> "**안 하면 아무것도 검증하지 못하기 때문**입니다.
>
> `@WebMvcTest`는 웹 계층만 올리는 슬라이스인데, **제가 만든 `SecurityConfig`를 자동으로 올려주지 않습니다.** 명시하지 않으면 Spring Boot의 기본 보안 설정으로 테스트하게 됩니다.
>
> 그러면 '401이 나온다'는 검증이 **제 설정 때문인지 스프링 기본값 때문인지 구분할 수 없습니다.** 심지어 제 설정에 버그가 있어도 테스트는 통과할 수 있습니다.
>
> 그래서 실제 `SecurityConfig`를 명시적으로 import해서, **테스트가 검증하는 대상이 제 설정이 맞도록** 했습니다. `SecurityTestSupportConfig`도 함께 import해서 JwtDecoder 같은 필요 빈을 공급합니다."

---

### Q9-3. 코드 리뷰나 검수는 어떻게 하셨나요?

**답변**

> "배포 전에 **별도 검수 단계**를 뒀고, 결과를 `REVIEW.md`로 남겼습니다. 🔴 blocker 3건, 🟡 개선 권장 9건, 🟢 확인 완료 10건입니다.
>
> **검수에서 신경 쓴 원칙**이 하나 있습니다. **'보고를 믿지 말고 직접 실행한다'** 입니다. 빌드가 통과한다는 말 대신 `./mvnw test`를 실제로 돌리고 결과를 표에 적었고, `.env`가 gitignore돼 있다는 말 대신 `git log --all --full-history -- '**/.env'`로 **커밋 이력에 한 번도 없었던 것까지** 확인했습니다.
>
> 그리고 **blocker 3건이 다 나온 후에 배포 승인을 받았습니다.** 중요한 건 그중 두 개(React 상태 버그, API 계약 불일치)가 **테스트로는 안 잡히는 종류**였다는 겁니다. CI는 전부 그린이었거든요. 그래서 **자동화 위에 사람의 시나리오 검수가 한 겹 더 필요**하다는 걸 배웠습니다."

---

### Q9-4. 커밋은 어떻게 관리하셨나요?

**답변**

> "**Conventional Commits**로 `feat:`, `fix:`, `docs:`, `test:`, `refactor:`, `chore:` 접두사를 붙이고, 한국어 본문으로 통일했습니다. 총 16개 커밋이고 **의미 단위로 하나씩** 나눴습니다.
>
> 커밋 단위가 계획서의 작업 청크와 거의 1:1로 대응합니다 — `chore: 프로젝트 스캐폴딩` → `feat: 도메인 엔티티·스키마 마이그레이션` → `feat: 벌금 계산 정책 + 단위 테스트` → `feat: JWT 인증·권한 설정` → ... 이런 식입니다.
>
> **시크릿 커밋은 0건**이고, 이력 전체를 검색해서 확인했습니다."

---

## 10. ★ 압박 질문 모음

> **압박 질문의 목적은 정답을 맞히는 게 아니라 "판단 근거가 있는가"를 보는 것이다.**
> 모르면 모른다고 하되, **왜 그렇게 했는지는 반드시 답한다.**

### Q10-1. "이 설계의 단점이 뭐라고 생각하세요?"

> "**세 가지**를 꼽겠습니다.
>
> **① 단일 스터디룸 전제가 코드 전반에 박혀 있습니다.**
> `STUDY_ROOM_ID = 1L` 상수가 서비스에 있고, `member` 테이블에 `study_room_id` FK가 없습니다. 멀티테넌시로 가려면 스키마와 쿼리를 전부 손봐야 합니다.
> 다만 **확장 경로는 막지 않았습니다.** `CHECK (id = 1)` 제약만 떼고 FK를 추가하면 되도록 설계했습니다. 그리고 이건 SPEC에 **명시적 비범위**로 적어둔 의도적 축소입니다. 멀티테넌시는 이미 다른 프로젝트에서 다뤘고, 이번엔 **벌금 스냅샷이라는 도메인 문제에 집중**하는 게 낫다고 봤습니다.
>
> **② 페이지네이션이 없습니다.**
> 멤버 목록과 회차 목록이 전체를 한 번에 줍니다. '스터디 규모는 수십 명'이라는 전제 위에서 내린 결정인데, 그 전제가 깨지면 바로 문제가 됩니다.
>
> **③ 동시성 처리가 얕습니다.**
> 출석 체크가 '조회 후 없으면 insert' 구조라, 같은 회차에 두 요청이 동시에 들어오면 유니크 제약 위반이 500으로 떨어집니다. 운영자가 1명이라 실 발생 확률은 낮지만, **'유니크 제약이 최종 방어선'이라고 문서에 써둔 만큼 그 방어선이 어떤 응답이 되는지는 정의돼야** 합니다. 409로 매핑하는 게 맞습니다."

**⚠️** 단점을 말할 때 **① 왜 그렇게 했는지 ② 어떻게 고칠지**를 반드시 붙인다. 단점만 나열하면 그냥 부족한 프로젝트가 된다.

---

### Q10-2. "왜 X 대신 Y를 안 썼나요?" 총정리

| 질문                                          | 핵심 답변                                                                                                                                                |
| --------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **왜 Prisma/TypeORM 대신 JPA?**               | Spring 표준 + ORM 다양성. 영속성 컨텍스트·더티 체킹은 Prisma에 없는 개념이라 새로 배웠다. 집계는 네이티브 쿼리로 내려가서 **JPA만 고집하지 않았다**      |
| **왜 Gradle 대신 Maven?**                     | Initializr 기본값, XML이라 명시적. **Gradle 경험은 아직 없고 다음 프로젝트에서 채울 계획**                                                               |
| **왜 jjwt 대신 Nimbus?**                      | Boot 4의 Jackson 3와 `jjwt-jackson`(Jackson 2 바인딩) 충돌. 부수적으로 **커스텀 필터를 안 만들어도 됐다**                                                |
| **왜 RS256 대신 HS256?**                      | 발급자=검증자가 같은 앱. RSA 키쌍 관리는 과설계. 다른 건 **키 배포 모델이지 알고리즘 강도가 아니다**                                                     |
| **왜 httpOnly 쿠키 대신 localStorage?**       | 크로스 오리진(Vercel↔Render) 구성에서 `SameSite=None`+CSRF 대응이 세트로 붙는다. S티어에서 12시간 만료로 절충하고 **README에 트레이드오프를 명시**       |
| **왜 Redis 캐싱을 안 넣었나?**                | 수천 행 규모에서 집계가 밀리초다. **캐시는 무효화 정책이라는 새 복잡도를 데려온다.** 워크스페이스 백로그에 미커버 개념으로 기록해뒀고 다음 프로젝트 후보 |
| **왜 Testcontainers 통합 테스트가 없나?**     | S티어 규정상 면제. **다른 프로젝트(`match-mate`, `pingboard`, `yuhyeon`)에서는 썼고**, 특히 match-mate에서는 쿼리 수 고정에 사용했다                     |
| **왜 커스텀 에러 DTO 대신 ProblemDetail?**    | RFC 9457 표준이고 Spring 내장. 같은 걸 또 만들 이유가 없다                                                                                               |
| **왜 커스텀 HealthController 대신 Actuator?** | 설정 두 줄이면 된다. `show-details` 미설정이라 내부 정보도 안 샌다                                                                                       |
| **왜 소셜 로그인이 없나?**                    | **닫힌 스터디 모임**이라 공개 회원가입 자체가 없다. 계정은 운영자가 만든다. 소셜 로그인은 `newGym`에서 카카오 OAuth로 다뤘다                             |

---

### Q10-3. "테스트가 28개면 너무 적지 않나요?"

> "**적습니다.** S티어 규정(하루 완성, 통합 테스트 면제)에서 내린 선택입니다.
>
> 다만 **'무엇을 덮었는가'로 봐주시면** 좋겠습니다. 이 프로젝트에서 틀리면 가장 아픈 건 벌금 계산과 권한 가드입니다. 그 둘을 우선 덮었고, 특히 **벌금 스냅샷 불변성**은 테스트로 고정했습니다.
>
> 그리고 **못 덮은 곳을 정확히 알고 있습니다.** 회차 API 권한 테스트가 통째로 없고, `AuthService` 자체도 목으로 대체돼서 '**admin이 bcrypt 정상 경로로 통과하는지**'가 자동 검증되지 않습니다. 코드 리뷰로는 확인했지만 데모 계정 규정의 핵심 보증이라 테스트로 못박는 게 맞습니다. 검수 문서에 🟡로 적어뒀고, 지금 추가한다면 이 두 개가 1순위입니다."

---

### Q10-4. "실제 사용자가 있나요? 없으면 의미 없는 프로젝트 아닌가요?"

> "**실사용자는 없습니다.** 포트폴리오 목적으로 만들었고, 그걸 숨기지 않겠습니다.
>
> 다만 이 프로젝트로 증명하려던 건 '사용자 수'가 아니라 **'요구사항을 받아서 판단하고 구현하는 과정'** 입니다.
>
> - 벌금 단가가 바뀌면 과거 기록은 어떻게 되어야 하나 → 스냅샷
> - 멤버를 지우면 벌금 근거는 어떻게 되나 → soft delete + FK RESTRICT
> - 멤버가 남의 벌금을 못 보게 하려면 → URL 구조 분리
>
> 전부 사용자 수와 무관하게 **답을 내야 하는 문제**들입니다.
>
> 참고로 실사용 프로젝트도 있습니다. `match-mate`는 제가 실제로 뛰는 풋살팀 운영에 쓰는 걸 전제로 만들었고, 데모 계정과 물리적으로 격리된 실제 팀 데이터가 공존합니다. 필요하시면 그쪽 이야기를 더 드릴 수 있습니다."

---

### Q10-5. "AI 도구를 썼나요?"

> "**썼습니다.** 설계 문서 작성, 보일러플레이트 생성, 코드 리뷰에 활용했습니다.
>
> 다만 **판단은 제가 했습니다.** 예를 들어 벌금을 스냅샷으로 저장할지 파생 계산할지, 소유권 격리를 비교문으로 할지 URL 구조로 할지 — 이런 건 도구가 대신 정해주지 않습니다. 그리고 그 판단의 근거를 **ADR 형태로 문서에 남겼습니다.**
>
> 오히려 AI를 쓰면서 배운 게 있습니다. **AI가 준 코드가 Spring Boot 3 기준인 경우가 많았습니다.** `authorizeRequests()`나 `spring-boot-starter-web`처럼 Boot 4에서 없어진 것들이요. 그래서 **공식 마이그레이션 문서로 검증하는 습관**이 생겼습니다.
>
> 그리고 배포 전 검수에서 blocker 3건을 찾았는데, 그중 React `key` 버그와 API 계약 불일치는 **코드만 봐서는 안 보이고 시나리오를 따라가야** 나오는 것들이었습니다. 도구가 코드를 빨리 만들어줄수록 **'실제로 동작하는지 끝까지 따라가는 검수'가 더 중요해진다**고 생각합니다."

---

### Q10-6. "이 프로젝트를 다시 만든다면 뭘 다르게 하시겠어요?"

> "**세 가지**입니다.
>
> ① **쿼리 수를 테스트로 고정**하겠습니다. 지금은 로그로 눈으로 확인했는데, Testcontainers로 실제 DB를 띄우고 Hibernate `Statistics`로 '멤버 3명일 때와 30명일 때 쿼리 수가 같다'를 어서션하겠습니다. 그래야 리팩터할 때 N+1이 다시 들어오는 걸 CI가 잡습니다.
>
> ② **`@LoginEmail` 커스텀 제약을 안 만드는 방향**을 먼저 고민하겠습니다. 지금 방식이 규정을 정확히 지키긴 하는데, 데모 계정 이메일을 `admin@studyfine.dev`처럼 **진짜 이메일 형식으로** 두면 예외 자체가 필요 없습니다. 규정이 `admin`으로 고정돼 있어서 커스텀 제약이 최선이었지만, **'예외를 만들지 않는 설계'가 우선**이라는 원칙은 유지하고 싶습니다.
>
> ③ **검수를 마지막이 아니라 중간에도** 넣겠습니다. 이번엔 전부 구현한 뒤에 검수해서 blocker 3건이 한꺼번에 나왔습니다. 특히 API 계약 불일치(#12↔#14)는 **API 명세를 확정한 직후에 두 엔드포인트를 나란히 놓고 봤다면** 구현 전에 잡을 수 있었습니다."

---

### Q10-7. "코드에 주석이 꽤 많던데, 과하지 않나요?"

> "**의도적으로 밀도를 정해뒀습니다.** 규칙이 이렇습니다.
>
> - 파일 상단에 **역할 1~2줄**
> - 공개 메서드에 **'왜'** 중심 JavaDoc
> - **비자명한 로직**에만 인라인 주석 (트랜잭션, 보안, 알고리즘)
> - **자명한 주석 금지** — 타입이 말해주는 걸 다시 쓰지 않음
>
> 예를 들어 이런 건 씁니다.
>
> ```java
> // JwtEncoderParameters.from(claims)만 쓰면 헤더 기본값이 RS256으로 잡혀 HS256 JWK와 맞지 않아
> // "Failed to select a JWK signing key"로 실패한다. HS256을 헤더에 명시해야 한다.
> ```
>
> 이건 **6개월 뒤의 제가(또는 다음 사람이) 이 줄을 지우려다 같은 함정에 빠지는 걸 막습니다.**
>
> 반대로 `i++ // i 증가` 같은 건 하나도 없습니다.
>
> 그리고 **라이브러리 깊은 해설은 코드가 아니라 별도 학습 문서(STUDY.md)가 담당**하게 했습니다. 면접관이 보는 코드는 프로답게 유지하고, 공부 내용은 문서로 분리한 겁니다."

---

### Q10-8. "Spring 처음 해보셨다면서 이 정도 설명이 가능한가요? 정말 본인이 하신 건가요?"

> "설계 판단은 제가 했고, 그 근거를 전부 문서로 남겼습니다. 확인하시고 싶으시면 몇 가지 방법이 있습니다.
>
> - **커밋 히스토리**를 보시면 작업 순서가 나옵니다. 스캐폴딩 → 엔티티/마이그레이션 → 벌금 정책+테스트 → 인증 → API → 프론트 → 배포 순입니다.
> - **`REVIEW.md`** 에는 검수에서 발견한 blocker 3건의 재현 절차와 수정 방향이 그대로 있습니다.
> - **막힌 지점들**을 구체적으로 말씀드릴 수 있습니다 — Jackson 3 import 문제, HS256 JWK 헤더 문제, CSRF 403, 메타스페이스 OOM. 전부 **에러 메시지와 원인과 해결책**을 기억합니다.
>
> 그리고 **모르는 건 모른다고 하겠습니다.** 예를 들어 Gradle은 안 써봤고, Spring의 트랜잭션 전파 옵션 7가지를 다 설명하라면 REQUIRED와 REQUIRES_NEW 정도까지가 제가 확실히 아는 범위입니다. **아는 범위와 모르는 범위를 구분해서 말하는 것**이 신입에게 더 중요하다고 생각합니다."

---

### Q10-9. "만약 이 서비스에 사용자가 10만 명이 되면 뭘 바꾸시겠어요?"

> "**바꿔야 하는 걸 순서대로** 말씀드리겠습니다. 다만 전제가 하나 있는데, 이 도메인은 '스터디 하나에 수십 명'이라 10만 명은 **'스터디룸이 수만 개'** 라는 뜻입니다. 그러면 먼저 **멀티테넌시**부터 필요합니다.
>
> **① 멀티테넌시** — `study_room` 단일 행 제약을 풀고, `member`/`study_session`에 `study_room_id`를 추가하고, 모든 쿼리에 테넌트 조건을 강제합니다. 이때 **한 곳이라도 조건을 빠뜨리면 다른 스터디 데이터가 새므로**, 조건을 개별 쿼리에 흩뿌리지 않고 Hibernate 필터나 리포지토리 기반 클래스로 강제하는 방식을 검토하겠습니다.
>
> **② 페이지네이션** — 지금은 전체를 한 번에 줍니다. 커서 기반으로 바꿉니다.
>
> **③ 집계 최적화** — 그때는 `SUM` 실시간 집계가 부담이 됩니다. 구체화 뷰나 집계 테이블을 도입합니다. **다만 여전히 `member.total_fine` 같은 컬럼은 안 만듭니다** — 갱신 책임이 여러 곳에 흩어지는 게 문제였으니, 갱신 주체를 한 곳(리프레시 잡)에 모으는 형태로 갑니다.
>
> **④ 인덱스 재검토** — `EXPLAIN ANALYZE`로 실제 쿼리 계획을 보고, 테넌트 컬럼이 포함된 복합 인덱스로 재설계합니다.
>
> **⑤ 캐싱** — 벌금 단가처럼 거의 안 바뀌고 자주 읽는 데이터는 Redis 캐싱 후보입니다. 다만 **캐시는 무효화 정책이라는 새 복잡도**를 데려오니 ①~④로 안 되는 게 확인된 뒤에 넣습니다.
>
> **⑥ 인프라** — 무상태 설계라 인스턴스 수평 확장은 그대로 됩니다. JWT라 세션 공유 문제가 없다는 게 여기서 이득이 됩니다.
>
> 핵심은 **①~②로 해결될 문제에 ⑤를 먼저 넣지 않는 것**이라고 생각합니다."

---

## 11. 내가 아는 한계 (먼저 말하기)

> **면접관이 지적하기 전에 먼저 말하면 신뢰가 생긴다.**
> 단, 반드시 **① 왜 지금 안 했는지 ② 어떻게 채울지**를 붙인다. 아래는 검수의 🟡 항목이다.

### 말할 가치가 높은 순서

**1. 회차 API 권한 가드 테스트 부재 (🟡-1)**

> "`POST /api/sessions`, `GET/DELETE /api/sessions/{id}`의 401/403이 테스트로 검증되지 않습니다. `@PreAuthorize`는 코드상 세 개 다 붙어 있는 걸 확인했지만, **테스트로 잠기지 않은 권한은 리팩터할 때 조용히 풀립니다.** 문서에는 '권한 매트릭스가 곧 테스트 명세'라고 써놓고 일부를 못 지킨 셈입니다. 기존 테스트 패턴을 복사하면 되니 1순위 개선 항목입니다."

**2. `AuthService` 미테스트 (🟡-2)**

> "컨트롤러 테스트가 `AuthService`를 목으로 대체해서, **'admin이 bcrypt 정상 경로로 통과하는지', '비활성 계정이 401인지', '실패 3종이 같은 메시지인지'** 가 자동 검증되지 않습니다. 코드 리뷰로는 확인했지만, **데모 계정 보안 원칙의 핵심 보증**이라 테스트로 못박는 게 맞습니다. `MemberRepository`와 `PasswordEncoder` 목만 있으면 되고 DB도 필요 없습니다."

**3. 로그인 타이밍 사이드 채널 (🟡-9)**

> "실패 메시지는 통일했는데 **응답 시간이 다릅니다.** 계정이 없으면 bcrypt 없이 즉시 실패하고, 있으면 bcrypt를 도니까요. 메시지만 맞추고 '끝'이라고 생각했던 게 안일했습니다. **사이드 채널은 응답 내용뿐 아니라 시간과 크기도 정보**라는 걸 배웠고, 표준 대응은 계정이 없을 때도 더미 해시로 비교를 한 번 돌리는 겁니다."

**4. 동시 요청 시 500 (🟡-5)**

> "출석 체크가 '조회 후 없으면 insert'라 동시 요청이 유니크 제약에 걸리면 `DataIntegrityViolationException`이 500으로 나갑니다. **API 문서에 없는 상태코드**입니다. 운영자 1명이라 실 발생 확률은 낮지만, '유니크 제약이 최종 방어선'이라고 써둔 만큼 **그 방어선이 어떤 응답이 되는지는 정의돼야** 합니다. 409로 매핑하는 게 맞습니다."

**5. 대시보드 총액 불일치 (🟡-3)**

> "운영자 대시보드의 '전체 누적 벌금'이 **활성 멤버만 합산**해서 실제보다 적게 나옵니다. **'과거 기록과 벌금은 보존한다'가 이 프로젝트의 핵심 가치인데 대표 숫자가 그 원칙과 모순**됩니다. 발견은 검수에서 했고 blocker는 아니라 남겼지만, 원칙과 어긋난다는 점에서 우선순위가 낮지 않습니다."

**6. 미체크와 정상 출석을 구분 못 함 (🟡-4)**

> "API는 아직 체크 안 한 멤버를 `status: null`로 내려주는데, 화면에서 `?? "PRESENT"`로 처리해서 **'정상' 배지로 칠해집니다.** 운영자가 **한 번도 체크 안 한 회차와 전원 정상 처리한 회차를 구분할 수 없습니다.** 서버가 구분해서 준 정보를 프론트가 버린 케이스입니다."

**7. 데모 시드가 기본 활성 (🟡-7)**

> "`APP_SEED_ENABLED`가 `matchIfMissing = true`라서 **환경변수를 안 넣으면 어디서든 자동 시드**됩니다. 샘플 멤버 비밀번호도 소스에 하드코딩돼 있고요. 데모 목적이고 권한이 MEMBER라 실질 위험은 낮지만, **'기본값이 안전한 쪽'이어야 한다는 원칙에는 어긋납니다.** 실배포 프로필에서 기본 `false`로 두는 게 맞습니다."

**8. 사용되지 않는 코드 (🟡-6)**

> "`Member.deactivate()`가 어디서도 호출되지 않습니다. 비활성화는 `updateProfile(null, null, false)` 경로로 처리됩니다. **'필요 없는 코드는 만들지 않는다'** 는 제 워크스페이스 원칙 위반이라 삭제하거나 통일해야 합니다."

---

### 한계를 말하는 프레임 (외워둘 것)

> **"[한계]가 있습니다. [왜 그렇게 됐는지]. 그래서 [어떤 리스크]가 있고, [이렇게 고치는 게] 맞다고 봅니다."**

예시:

> "쿼리 수를 자동 검증하지 못합니다. 실제 DB가 필요해서 S티어 범위에서 뺐기 때문입니다. 그래서 **리팩터하다가 누가 `@EntityGraph`를 지워도 CI가 안 잡아주는** 리스크가 있습니다. Testcontainers로 Hibernate `Statistics`를 어서션하는 게 맞고, 다른 프로젝트에서는 실제로 그렇게 했습니다."

---

## 12. 역질문 / 마무리

### 면접 끝에 할 질문 (준비해가면 좋은 것)

**기술/개발문화**

- "팀에서 Spring Boot는 몇 버전을 쓰고 계신가요? Boot 4 마이그레이션 계획이 있으신지 궁금합니다."
- "ORM은 JPA를 쓰시나요? 복잡한 조회는 어떻게 처리하시는지 (QueryDSL, MyBatis, 네이티브) 궁금합니다."
- "N+1이나 슬로우 쿼리를 발견하는 프로세스가 따로 있나요? APM을 쓰시는지요."
- "DB 마이그레이션은 어떻게 관리하시나요? Flyway/Liquibase 쓰시나요?"
- "테스트는 어느 수준까지 요구되나요? 통합 테스트에 실제 DB를 쓰시는지 궁금합니다."

**성장/온보딩**

- "신입이 들어오면 처음 몇 달간 어떤 일을 맡게 되나요?"
- "코드 리뷰 문화는 어떤가요? 리뷰에서 주로 어떤 점을 보시나요?"
- "제가 입사 전까지 준비하면 좋을 게 있다면 무엇일까요?"

**⚠️** 복지·연봉만 묻지 않는다. 기술 질문을 최소 2개는 준비한다.

---

### 마무리 어필 (시간이 남으면)

> "이 프로젝트는 규모는 작지만, **판단이 필요한 지점마다 근거를 남기는 연습**을 했습니다. 벌금을 스냅샷으로 저장할지, 소유권을 비교문으로 막을지 구조로 막을지, JVM을 512MB에 올릴 수 있는지 — 전부 정답이 정해져 있지 않은 문제였고, 각각에 대해 왜 그 선택을 했는지 문서와 코드 주석에 남겼습니다.
>
> 그리고 **모르는 걸 모른다고 기록하는 것**도 같이 했습니다. 검수에서 나온 개선 항목 9건을 지우지 않고 문서에 남겨뒀습니다. 완성도를 높이는 것만큼 **지금 어디까지 왔는지 정확히 아는 것**이 중요하다고 생각합니다."

---

## 13. 절대 하면 안 되는 답변

| 상황                               | ❌ 이렇게 말하면 감점               | ✅ 이렇게                                                                                                                                                 |
| ---------------------------------- | ----------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 모르는 걸 물었을 때                | "아 그건 잘..." 하고 얼버무리기     | **"그건 아직 안 써봤습니다. 다만 제가 아는 범위에서는 [관련 개념]이고, 이렇게 접근해볼 것 같습니다."**                                                    |
| Gradle 질문                        | "Gradle도 비슷해요" (안 써봤으면서) | **"Maven만 써봤습니다. 다음 프로젝트에서 Gradle+Kotlin DSL로 채울 계획입니다."**                                                                          |
| CSRF를 껐다고 지적당할 때          | "JWT라서 괜찮습니다" (근거 없이)    | **"세션 쿠키를 안 쓰고 헤더 인증만 하므로 브라우저 자동 전송이 없어 CSRF가 성립하지 않습니다. 대신 XSS 노출이라는 대가가 있고 README에 명시했습니다."**   |
| CSRF 기본값 사실관계로 반박당할 때 | 우기기                              | **"맞습니다, Security의 기본 동작이었습니다. 제가 부딪힌 실제 증상과 결론(명시적 disable + 근거 주석)은 같습니다."**                                      |
| 스냅샷을 비정규화라고 지적당할 때  | "성능 때문에요"                     | **"성능이 아니라 도메인 정확성 때문입니다. FineRate와 Fine은 서로 다른 개념이고, 저장 안 하면 과거 벌금이 소급 인상됩니다."**                             |
| 테스트가 적다고 할 때              | "시간이 없어서요"                   | **"S티어 범위에서 핵심 두 곳(벌금 계산·권한 가드)을 우선 덮었습니다. 못 덮은 곳은 이것들이고 1순위 개선 항목입니다."**                                    |
| AI 사용                            | 숨기기                              | **"썼습니다. 다만 설계 판단은 제가 했고 ADR로 남겼습니다. AI가 준 Boot 3 코드가 Boot 4에서 안 되는 걸 여러 번 겪어서 공식 문서 검증 습관이 생겼습니다."** |
| 프론트 가드에 대해                 | "이중 방어입니다"                   | **"프론트 가드는 UX일 뿐 보안 경계가 아닙니다. 실제 방어선은 서버 `@PreAuthorize`입니다."**                                                               |
| 데모 계정 지적                     | "포폴이라 그냥 넣었어요"            | **"비밀번호 검증은 우회하지 않고, 미인증 토큰 발급 엔드포인트도 없습니다. 형식 검증 예외는 리터럴 1건·DTO 1곳으로 격리했습니다."**                        |
| 단점 질문                          | 단점만 나열                         | **단점 → 왜 그렇게 했는지 → 어떻게 고칠지, 3단으로**                                                                                                      |
| 다른 기술 비교                     | "NestJS가 더 좋아요/나빠요"         | **"용도가 다릅니다. NestJS는 [장점], Spring은 [장점]. 이번엔 [이유]로 Spring을 골랐습니다."**                                                             |

---

## 부록. 30초 안에 말해야 하는 핵심 3개

면접에서 시간이 없을 때 **이것만은 반드시** 전달한다.

### 1. 벌금 스냅샷

> "벌금을 조회 시점에 계산하지 않고 **출석 체크 시점의 단가로 확정 저장**합니다. 단가를 올려도 과거 회차 금액이 소급 인상되지 않습니다. 주문서에 주문 당시 가격을 박아두는 것과 같은 이유입니다. **단가 변경 서비스가 출석 리포지토리를 참조조차 못 하게** 의존 방향을 잘라서 소급 오염 경로를 코드에서 없앴고, 그 불변식을 테스트로 고정했습니다."

### 2. N+1 방지

> "멤버별 누적 벌금은 **GROUP BY 프로젝션 단일 쿼리**, 회차 상세는 **`@EntityGraph`**, 일괄 저장은 **사전 조회 + Map**으로 각각 막았습니다. 출석 체크 루프 안에는 리포지토리 호출이 0건이라, **멤버가 4명이든 40명이든 쿼리 수가 같습니다.** `open-in-view: false`로 지연 로딩이 숨겨지는 것도 차단했습니다."

### 3. 소유권 격리를 구조로

> "본인 조회는 `/api/me/attendances`처럼 **id 파라미터를 아예 안 받고** 토큰의 `sub`만 씁니다. id를 받는 경로는 통째로 ORGANIZER 전용이고요. 그래서 '**소유권 비교를 빠뜨려서 생기는 수평 권한 상승**'이 구조적으로 불가능합니다. 비교문 하나를 잊으면 뚫리는 설계를 아예 안 만든 겁니다."

---

_개념 설명은 [STUDY.md](./STUDY.md), 코드 근거는 [ARCHITECTURE.md](./ARCHITECTURE.md) · [DATA-MODEL.md](./DATA-MODEL.md) · [API.md](./API.md) · [REVIEW.md](./REVIEW.md)를 본다._
